// LUNA — Module: knowledge — Constants
import { resolve } from 'node:path'

/** Absolute path to the knowledge media directory on disk. */
export const KNOWLEDGE_MEDIA_DIR = resolve(process.cwd(), 'instance/knowledge/media')
