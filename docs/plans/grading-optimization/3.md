# Plan 3: Console, Operacional y Documentación

## Objetivo
Actualizar la UI de console para el modelo single-framework, optimizar queries operacionales con paginación, limpiar API routes obsoletos, y actualizar toda la documentación.

## Pre-requisitos
- **Plan 1 completado** — config simplificado, tipos actualizados, nuevo formato qualifying.json.

## Contexto para el ejecutor

Con Plan 1 completado, la config ahora es single-framework con priority-based weights. La UI de console necesita reflejar esto: en vez de 3 cards de framework con toggles, ahora hay un selector de preset + edición de criterios inline. Los API routes de multi-framework (`set-framework`, `reset-framework`, `frameworks`) fueron reemplazados por `presets` y `apply-preset`.

Las queries de batch recalculation cargan todos los leads sin LIMIT, lo cual es un riesgo operacional con bases grandes.

## Cambios detallados

### 1. `src/modules/lead-scoring/templates.ts` — Simplificar SSR

**Eliminar:**
- Toda la lógica de framework cards (toggle per-framework, multi-framework note)
- Labels de `fw_spin`, `fw_champ`, `fw_champ_gov`, `fw_multi_note`
- HTML de cards con checkboxes para enable/disable frameworks
- JavaScript de `setFramework()`, `resetFramework()`

**Agregar:**
- Sección "Preset activo" con selector dropdown: SPIN (B2C), CHAMP (B2B), CHAMP+Gov (B2G)
- Botón "Aplicar preset" que llama POST `/apply-preset` (reemplaza criterios, stages, etc.)
- Confirmation dialog antes de aplicar ("Esto reemplazará los criterios actuales. ¿Continuar?")
- Label del objetivo activo junto al preset: `schedule | sell | escalate | attend_only`
- Selector de objetivo (dropdown) que se guarda junto con la config

**Modificar la tabla de criterios:**
- Reemplazar columna "Peso" (weight: number input) con columna "Prioridad" (priority: select alta/media/baja)
- Agregar columna "Scoring" para enum types: select con opciones "Orden" (indexed) y "Presencia" (presence)
- El input de peso numérico desaparece completamente
- Eliminar la fila de "Total de pesos" (ya no aplica)
- Agregar indicador visual: máximo 10 criterios. Mostrar "X/10 criterios" y deshabilitar botón "Agregar" si está lleno.

**Modificar la sección de comportamiento (behavior tab):**
- Agregar campo `dataFreshnessWindowDays` (input numérico, label: "Ventana de frescura de datos (días)")
- Agregar info text: "Los datos de calificación pierden relevancia con el tiempo. Después de este periodo, contribuyen solo 30% al score."
- `minConfidence` ya existe, ajustar default visual a 0.4

**Variables de labels a actualizar:**
```typescript
// Nuevas
preset_label: 'Preset activo' / 'Active preset'
preset_apply: 'Aplicar preset' / 'Apply preset'
preset_confirm: 'Esto reemplazará los criterios actuales. ¿Continuar?' / 'This will replace current criteria. Continue?'
objective_label: 'Objetivo' / 'Objective'
priority_high: 'Alta' / 'High'
priority_medium: 'Media' / 'Medium'
priority_low: 'Baja' / 'Low'
scoring_indexed: 'Orden' / 'Indexed'
scoring_presence: 'Presencia' / 'Presence'
criteria_count: 'criterios' / 'criteria'
freshness_label: 'Frescura de datos (días)' / 'Data freshness (days)'
freshness_info: 'Después de este periodo, los datos contribuyen solo 30% al score' / 'After this period, data contributes only 30% to score'

// Eliminar
fw_spin, fw_champ, fw_champ_gov, fw_spin_type, fw_champ_type, fw_champ_gov_type
fw_spin_desc, fw_champ_desc, fw_champ_gov_desc
fw_view, fw_hide, fw_multi_note, fw_confirm, fw_applied, fw_apply_error
weight_total
```

### 2. `src/modules/lead-scoring/ui/lead-scoring.html` — Actualizar frontend

**Cambios principales:**
- Actualizar JavaScript para llamar a los nuevos endpoints (`/presets`, `/apply-preset` en vez de `/frameworks`, `/set-framework`)
- Actualizar el rendering de criterios para usar `priority` en vez de `weight`
- Agregar select de `enumScoring` en cada fila de criterio tipo enum
- Agregar campo `dataFreshnessWindowDays` en la sección de comportamiento
- Actualizar la función `saveConfig()` para enviar el nuevo formato de QualifyingConfig
- Al agregar un criterio nuevo: asignar `priority: 'medium'` por defecto (en vez de `weight: 0`)
- Validación client-side: max 10 criterios, essentialQuestions max 2

**Nota:** Revisar el archivo HTML actual (`ui/lead-scoring.html`) completamente. Es SSR + JS vanilla. Asegurarse de que el JS que maneja las interacciones (save, apply preset, add criterion) use los nuevos endpoints y formatos.

### 3. `src/modules/lead-scoring/pg-queries.ts` — Cursor-based batch

**Modificar `getAllLeadsForRecalc()`:**

```typescript
async getAllLeadsForRecalc(batchSize = 100): AsyncGenerator<Array<{
  contactId: string
  qualificationData: Record<string, unknown>
  qualificationStatus: QualificationStatus
}>> {
  let offset = 0

  while (true) {
    const result = await this.db.query(
      `SELECT ac.contact_id,
              COALESCE(ac.qualification_data, '{}') AS qualification_data,
              ac.lead_status AS qualification_status
       FROM agent_contacts ac
       JOIN contacts c ON c.id = ac.contact_id
       WHERE c.contact_type = 'lead'
         AND ac.lead_status NOT IN ('blocked', 'converted')
       ORDER BY ac.contact_id
       LIMIT $1 OFFSET $2`,
      [batchSize, offset],
    )

    if (result.rows.length === 0) break

    yield result.rows.map((r: { contact_id: string; qualification_data: unknown; qualification_status: string }) => ({
      contactId: r.contact_id,
      qualificationData: typeof r.qualification_data === 'string'
        ? JSON.parse(r.qualification_data)
        : r.qualification_data as Record<string, unknown>,
      qualificationStatus: r.qualification_status as QualificationStatus,
    }))

    offset += batchSize
  }
}
```

**Nota:** Cambiar de función regular a `AsyncGenerator`. Si esto genera demasiada fricción en callers, alternativamente mantenerla como función normal pero agregar parámetro `limit` con default 500 y iterar desde el caller.

**Alternativa más simple (preferida si el ejecutor lo ve más pragmático):**

Mantener la firma original pero agregar paginación interna:

```typescript
async getAllLeadsForRecalc(): Promise<Array<{...}>> {
  const allLeads: Array<{...}> = []
  let offset = 0
  const batchSize = 200

  while (true) {
    const result = await this.db.query(
      `SELECT ... LIMIT $1 OFFSET $2`,
      [batchSize, offset],
    )
    if (result.rows.length === 0) break
    allLeads.push(...result.rows.map(/* ... */))
    offset += batchSize
    if (allLeads.length >= 10000) break  // safety cap
  }

  return allLeads
}
```

**Actualizar `batchUpdateScores()`:** Sigue igual (ya maneja transacciones correctamente), pero actualizar la llamada a `calculateScore()` para no pasar framework override (ya no existe).

**Actualizar callers:**
- `manifest.ts` en el hook `console:config_applied`: actualizar llamada a `calculateScore(data, config)` sin tercer parámetro de framework.
- `manifest.ts` en la ruta POST `/recalculate`: idem.

**Quitar `'directo'` del filtro NOT IN de `getAllLeadsForRecalc()`:** el estado `directo` debería recalcularse también (podría cambiar con nueva config). Solo excluir `blocked` y `converted` que son terminales.

### 4. `src/modules/lead-scoring/manifest.ts` — Limpiar API routes

**Actualizar rutas según lo definido en Plan 1, sección 5.** Verificar que:

- `GET /presets` devuelve lista de presets con info (nombre, descripción, criteriaCount, stageCount)
- `POST /apply-preset` acepta `{ preset: string }`, aplica el preset al config, guarda, limpia
- `POST /recalculate` usa `calculateScore(data, config)` sin framework override
- `PUT /lead-status` usa `calculateScore(data, config)` sin framework override
- `GET /config` retorna nuevo formato QualifyingConfig
- `PUT /config` valida y guarda nuevo formato

**Eliminar:**
- Ruta `POST /set-framework`
- Ruta `POST /reset-framework`
- Ruta `GET /frameworks`

### 5. `src/modules/lead-scoring/CLAUDE.md` — Reescribir

Reescribir completamente para reflejar v3:

```markdown
# Lead Scoring — Sistema de calificación de leads (v3)

Califica leads usando un framework configurable (presets: CHAMP/SPIN/CHAMP+Gov o custom).
Un framework activo por tenant. Extracción code-only (sin LLM interno), scoring determinístico,
decay temporal, priority weights. UI en console.

## Archivos
- `manifest.ts` — lifecycle, configSchema, console (apiRoutes), servicios
- `types.ts` — QualifyingConfig, QualifyingCriterion (priority-based), QualificationStatus, ScoreResult
- `frameworks.ts` — presets CHAMP, SPIN, CHAMP+Gov (max 10 criterios cada uno)
- `scoring-engine.ts` — calculateScore(), buildQualificationSummary(), temporal decay, transition validation
- `config-store.ts` — lee/escribe instance/qualifying.json, migraciones old→v3, apply preset
- `extract-tool.ts` — tool `extract_qualification`: recibe datos estructurados del agentic loop, merge + score (CERO LLM)
- `pg-queries.ts` — queries paginadas: listar leads, detalle, actualizar score, recalcular batch, stats
- `templates.ts` — SSR HTML: preset selector, criterios con priority, comportamiento

## Manifest
- type: `feature`, removable: true, activateByDefault: true
- depends: `['tools']`

## Servicios registrados
- `lead-scoring:config` — instancia de ConfigStore
- `lead-scoring:queries` — instancia de LeadQueries

## Extracción (Zero-LLM)
- Tool `extract_qualification` recibe `{ extracted, confidence, disqualify_reason? }` del agentic loop
- NO hace llamadas LLM internas. Solo merge transaccional + calculateScore + transition
- El agentic loop extrae datos naturalmente y los pasa como parámetros del tool
- Keys que no coinciden con criterios configurados se ignoran silenciosamente

## Scoring
- Pesos por prioridad: high=3, medium=2, low=1 → normalizados a 100
- Enum scoring: 'indexed' (posición = calidad) o 'presence' (cualquier valor = full score)
- Decay temporal: datos pierden relevancia linealmente (100%→30% en dataFreshnessWindowDays)
- Timestamps en `_extracted_at` por campo

## Config (instance/qualifying.json)
- `preset`: preset base ('champ', 'spin', 'champ_gov', null)
- `objective`: 'schedule' | 'sell' | 'escalate' | 'attend_only'
- `criteria[]`: max 10, con priority en vez de weight
- `thresholds`: cold/qualifying/qualified
- `dataFreshnessWindowDays`: default 90

## API routes (montadas en /console/api/lead-scoring/)
- `GET /config`, `PUT /config`, `GET /presets`, `POST /apply-preset`
- `POST /recalculate`, `GET /stats`, `GET /stats-detailed`
- `GET /leads`, `GET /lead`, `PUT /lead-status`, `POST /disqualify`

## Trampas
- Config en instance/qualifying.json (JSON), NO en .env
- Keys auto-generadas desde name.en via generateKeyFromName()
- Configs viejas (BANT plano, multi-framework v2) se migran automáticamente a v3
- `_extracted_at`, `_confidence` y `_disqualified` son campos reservados en qualification_data
- Max 10 criterios por tenant
- Helpers HTTP: usa jsonResponse, parseBody, parseQuery de kernel/http-helpers.js
```

### 6. `CLAUDE.md` (raíz) — Actualizar entrada

En la sección "Módulos documentados", la entrada de lead-scoring dice:
```
- `src/modules/lead-scoring/CLAUDE.md` — calificación de leads (BANT + custom, scoring, UI console)
```

Cambiar a:
```
- `src/modules/lead-scoring/CLAUDE.md` — calificación de leads (CHAMP/SPIN presets, scoring determinístico zero-LLM, priority weights, decay temporal)
```

En la sección "Arquitectura" y cualquier referencia a BANT, actualizar a "framework configurable con presets (CHAMP/SPIN/CHAMP+Gov)".

### 7. `docs/architecture/lead-status.md` — Actualizar si Plan 1 no lo completó

Verificar que este archivo refleja:
- Framework configurable (no BANT)
- Priority weights (no peso manual)
- Extracción zero-LLM
- Decay temporal
- Max 10 criterios
- `qualified_action` ahora se llama `objective` y está a nivel de config (no de framework)

Si Plan 1 ya lo actualizó, verificar que está completo. Si no, completar.

## Verificación

1. `npx tsc --noEmit` compila limpio
2. Console UI muestra selector de preset, criterios con priority, campo de frescura de datos
3. "Aplicar preset" reemplaza criterios y stages correctamente
4. Agregar criterio funciona hasta el límite de 10, botón se deshabilita
5. Enum criteria muestran selector de scoring mode (indexed/presence)
6. "Recalcular" funciona con paginación (no carga todo a memoria)
7. API routes obsoletos (set-framework, reset-framework, frameworks) retornan 404
8. CLAUDE.md del módulo refleja v3
9. CLAUDE.md raíz tiene entrada actualizada

## Notas para el ejecutor

- El archivo `templates.ts` es largo (~600+ líneas de HTML generado via template strings). Hay que leerlo completo antes de modificar para entender la estructura del SSR.
- El archivo `ui/lead-scoring.html` también es extenso. Leerlo completo.
- La relación entre `templates.ts` (SSR server-side) y `ui/lead-scoring.html` (static HTML) puede ser confusa. `templates.ts` genera HTML que se inyecta en la consola como sección embebida. `ui/lead-scoring.html` es un HTML standalone que se sirve como página completa via GET `/ui`. Verificar cuál se usa realmente en la console actual (probablemente ambos: el HTML para la página standalone y templates.ts para la sección embebida).
- Para `getAllLeadsForRecalc()`, la alternativa simple (array con paginación interna) es preferida sobre AsyncGenerator por simplicidad de callers.
- Al limpiar API routes en manifest.ts, verificar que el frontend (lead-scoring.html y templates.ts JS) no llame a los routes eliminados. Actualizar cualquier referencia.
