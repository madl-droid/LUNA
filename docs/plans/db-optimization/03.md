# Plan 3: Migration Squash

**Tipo:** SQL cleanup + documentacion
**Dependencias:** Ninguna
**Estado:** `001_beta-schema.sql` ya fue escrito (1992 lineas, 78 tablas). Solo falta cleanup.

---

## Objetivo

Reemplazar los 51 archivos de migracion con el unico `001_beta-schema.sql` que ya existe.

---

## PRE-COMPLETADO (ya hecho por el planner)

- [x] `001_beta-schema.sql` escrito y verificado (78 tablas, pgvector, seeds)
- [x] `kernel/db.ts` limpiado (inline DDL + seedDefaultAgent eliminados)

---

## Pasos de ejecucion

### Paso 1: Eliminar las 51 migraciones viejas

```bash
cd src/migrations/
# Listar lo que hay
ls *.sql | wc -l
# Debe ser 55 archivos (51 originales + 001_beta-schema.sql + 4 pares de duplicados)

# Eliminar TODOS excepto 001_beta-schema.sql
ls *.sql | grep -v "001_beta-schema.sql" | xargs rm
ls *.sql
# Debe quedar SOLO: 001_beta-schema.sql
```

**IMPORTANTE:** git add + commit despues de este paso. Si algo falla, el cleanup es revertible.

### Paso 2: Verificar que el migrador funciona con un solo archivo

Leer `src/kernel/migrator.ts` y verificar:
- Ordena por nombre de archivo → `001_beta-schema.sql` es primero (y unico)
- No tiene referencias hardcoded a migraciones especificas
- `schema_migrations` se crea antes de ejecutar migraciones
- `INSERT ON CONFLICT DO NOTHING` previene re-ejecucion

**NO modificar migrator.ts** — solo verificar.

### Paso 3: Actualizar CLAUDE.md raiz

En `/CLAUDE.md`, seccion "Sistema de migraciones SQL":

**REEMPLAZAR** la lista de archivos de migracion con:

```
001_beta-schema.sql  — Schema completo para beta: 78 tablas, extensions, indexes, FKs, seeds
```

**ELIMINAR** las notas sobre:
- "4 pares de numeros duplicados"
- "~52 tablas creadas por modulos en init()"
- La lista detallada de migraciones individuales

**ACTUALIZAR** la nota sobre modulos:
```
- Los módulos ya NO crean tablas en init() — ver Plan 4
```

Dejar intacta la seccion de "Como agregar una nueva migracion".

### Paso 4: Verificar que no haya referencias a migraciones eliminadas

```bash
grep -rn "002_memory\|003_memory\|004_memory\|005_proactive\|006_ack\|007_rename\|008_replan\|009_db-cleanup\|010_alter\|011_cross\|012_task\|013_subagent" src/ --include="*.ts" | grep -v node_modules | grep -v migrations
```

**Resultado esperado:** CERO referencias. Si hay alguna, es un bug — reportar.

### Paso 5: Compilar TypeScript

```bash
docker run --rm -v /docker/luna-repo:/app -w /app node:22-alpine npx tsc --noEmit
```

Si hay errores, corregirlos. El archivo 001_beta-schema.sql NO deberia causar errores de TS porque es SQL puro y migrator.ts solo lo lee como string.

---

## Criterios de completitud

- [x] `001_beta-schema.sql` existe con 78 tablas
- [x] `kernel/db.ts` limpiado
- [ ] 51 migraciones viejas eliminadas
- [ ] Solo queda `001_beta-schema.sql` en `src/migrations/`
- [ ] CLAUDE.md raiz actualizado
- [ ] TypeScript compila sin errores
- [ ] Grep por nombres de migraciones viejas = 0 resultados

---

## REGLAS para el ejecutor

1. **NO modificar `001_beta-schema.sql`** — ya fue verificado contra PostgreSQL
2. **NO modificar `kernel/migrator.ts`** — solo verificar que funciona
3. **NO modificar `kernel/db.ts`** — ya fue limpiado
4. **Commit despues de cada paso** para minimizar riesgo
5. **Si encuentra un archivo .ts que importa un nombre de migracion especifica**, es un bug — corregirlo

---

## Resumen de lo que contiene 001_beta-schema.sql

- CREATE EXTENSION vector
- CREATE FUNCTION update_updated_at()
- 78 CREATE TABLE IF NOT EXISTS (orden correcto para FKs)
- Todos los indexes (btree, GIN, HNSW, IVFFLAT)
- Todos los UNIQUE constraints
- Todos los CHECK constraints
- Todos los FOREIGN KEY constraints
- Triggers (update_updated_at)
- Seeds: campaign default, ack_messages (10), subagents (4: web-researcher, medilink-scheduler, google-calendar-scheduler, comparativo-researcher)

### Tablas eliminadas (NO presentes):
- `agents` — vestigial
- `task_checkpoints` — feature cancelada
- `session_summaries` (v1) — reemplazada por v2
- `conversation_archives` (v1) — reemplazada por session_archives
- `summary_chunks` — absorbida en session_memory_chunks

### Correcciones aplicadas:
- TEXT→UUID: session_archives.session_id, session_summaries_v2.session_id, session_memory_chunks.session_id, hitl_tickets.requester_contact_id, hitl_tickets.session_id, calendar_follow_ups.contact_id, sessions.campaign_id
- FKs nuevas: sessions→campaigns, session_archives→contacts, session_summaries_v2→contacts, session_memory_chunks→contacts, calendar_follow_ups→contacts
