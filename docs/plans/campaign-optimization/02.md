# Plan 2: Enhanced Metrics + Console UI + Documentation

## Objetivo
Agregar metricas desglosadas por fuente de match (keyword/UTM/webhook), actualizar la consola con nuevos campos (utm_keys, origin badge, toggles admin, stats expandidos), implementar first-touch attribution, y actualizar documentacion.

## Dependencia
**Requiere Plan 1 completado.** Usa columnas `match_source`, `utm_data` en `contact_campaigns`, y `utm_keys`, `origin` en `campaigns`.

---

## Tarea 1: Tipos de stats expandidos

**Archivo**: `src/modules/marketing-data/campaign-types.ts`

### 1a. Nuevo tipo `CampaignDetailedStats`
Agregar al final del archivo:

```typescript
export interface SourceBreakdown {
  matchSource: string          // 'keyword' | 'url_utm' | 'webhook' | 'webhook_utm'
  entries: number
  conversions: number
}

export interface UtmBreakdown {
  utmSource: string            // valor de utm_source (o 'unknown')
  utmMedium: string            // valor de utm_medium (o 'unknown')
  entries: number
  conversions: number
}

export interface CampaignDetailedStats extends CampaignStatRow {
  sourceBreakdown: SourceBreakdown[]
  utmBreakdown: UtmBreakdown[]
  firstTouchEntries: number    // leads cuyo PRIMER contacto fue esta campana
}
```

---

## Tarea 2: Stats queries expandidas

**Archivo**: `src/modules/marketing-data/campaign-queries.ts`

### 2a. Nuevo metodo `getCampaignDetailedStats`

Agregar a la clase `CampaignQueries`:

```typescript
async getCampaignDetailedStats(): Promise<CampaignDetailedStats[]>
```

Este metodo reutiliza `getCampaignStats()` como base y agrega:

**Query 1 — Source breakdown por campana:**
```sql
SELECT campaign_id,
       COALESCE(match_source, 'keyword') AS match_source,
       COUNT(DISTINCT contact_id) AS entries
FROM contact_campaigns
GROUP BY campaign_id, match_source
```

**Query 2 — UTM breakdown por campana:**
```sql
SELECT campaign_id,
       COALESCE(utm_data->>'utm_source', 'unknown') AS utm_source,
       COALESCE(utm_data->>'utm_medium', 'unknown') AS utm_medium,
       COUNT(DISTINCT contact_id) AS entries
FROM contact_campaigns
WHERE utm_data != '{}'::jsonb AND utm_data IS NOT NULL
GROUP BY campaign_id, utm_data->>'utm_source', utm_data->>'utm_medium'
```

**Query 3 — First-touch attribution:**
```sql
WITH first_campaign AS (
  SELECT DISTINCT ON (contact_id) contact_id, campaign_id
  FROM contact_campaigns
  ORDER BY contact_id, matched_at ASC
)
SELECT campaign_id, COUNT(*) AS first_touch_entries
FROM first_campaign
GROUP BY campaign_id
```

**Query 4 — Conversiones por source (para source breakdown):**
```sql
WITH last_campaign AS (
  SELECT DISTINCT ON (contact_id) contact_id, campaign_id, match_source
  FROM contact_campaigns
  ORDER BY contact_id, matched_at DESC
)
SELECT lc.campaign_id, COALESCE(lc.match_source, 'keyword') AS match_source, COUNT(*) AS conversions
FROM last_campaign lc
JOIN agent_contacts ac ON ac.contact_id = lc.contact_id
WHERE ac.lead_status = 'converted'
GROUP BY lc.campaign_id, lc.match_source
```

**Query 5 — Conversiones por UTM source (para utm breakdown):**
```sql
WITH last_campaign AS (
  SELECT DISTINCT ON (contact_id) contact_id, campaign_id, utm_data
  FROM contact_campaigns
  ORDER BY contact_id, matched_at DESC
)
SELECT lc.campaign_id,
       COALESCE(lc.utm_data->>'utm_source', 'unknown') AS utm_source,
       COALESCE(lc.utm_data->>'utm_medium', 'unknown') AS utm_medium,
       COUNT(*) AS conversions
FROM last_campaign lc
JOIN agent_contacts ac ON ac.contact_id = lc.contact_id
WHERE ac.lead_status = 'converted' AND lc.utm_data != '{}'::jsonb
GROUP BY lc.campaign_id, lc.utm_data->>'utm_source', lc.utm_data->>'utm_medium'
```

**Ensamblaje**: Hacer las 5 queries en paralelo con `Promise.all()`, luego cruzar resultados en memoria para construir el array de `CampaignDetailedStats`.

**NOTA**: Mantener el metodo `getCampaignStats()` existente sin cambios (backward compat). El nuevo metodo es un endpoint separado.

### 2b. Nuevo metodo `getGlobalUtmBreakdown`

Para una vista global (no por campana) de fuentes UTM:

```typescript
async getGlobalUtmBreakdown(): Promise<UtmBreakdown[]>
```

```sql
SELECT COALESCE(utm_data->>'utm_source', 'unknown') AS utm_source,
       COALESCE(utm_data->>'utm_medium', 'unknown') AS utm_medium,
       COUNT(DISTINCT contact_id) AS entries
FROM contact_campaigns
WHERE utm_data != '{}'::jsonb AND utm_data IS NOT NULL
GROUP BY utm_data->>'utm_source', utm_data->>'utm_medium'
ORDER BY entries DESC
LIMIT 50
```

Agregar conversiones con un LEFT JOIN similar a las queries anteriores.

---

## Tarea 3: API Routes nuevos

**Archivo**: `src/modules/marketing-data/manifest.ts`

Agregar a `createApiRoutes()`:

### 3a. GET campaign-detailed-stats
```typescript
{
  method: 'GET',
  path: 'campaign-detailed-stats',
  handler: async (_req, res) => {
    try {
      const stats = await getCampaignQueries().getCampaignDetailedStats()
      jsonResponse(res, 200, { stats })
    } catch (err) {
      jsonResponse(res, 500, { error: String(err) })
    }
  },
}
```

### 3b. GET utm-breakdown
```typescript
{
  method: 'GET',
  path: 'utm-breakdown',
  handler: async (_req, res) => {
    try {
      const breakdown = await getCampaignQueries().getGlobalUtmBreakdown()
      jsonResponse(res, 200, { breakdown })
    } catch (err) {
      jsonResponse(res, 500, { error: String(err) })
    }
  },
}
```

### 3c. GET config (para leer toggles)
```typescript
{
  method: 'GET',
  path: 'config',
  handler: async (_req, res) => {
    // Leer config actual del module
    // Esto depende de como se implementaron los toggles en Plan 1
    // Si son env vars, retornar sus valores actuales
    jsonResponse(res, 200, {
      utmMatchEnabled: config.CAMPAIGN_UTM_MATCH_ENABLED,
      keywordMatchEnabled: config.CAMPAIGN_KEYWORD_MATCH_ENABLED,
    })
  },
}
```

**Nota sobre la variable `config`**: El handler necesita acceso a la config del modulo. Patron: en `init()`, guardar la config en una variable de modulo (como se hace con `campaignQueries`). La variable `config` ya deberia existir del Plan 1, y solo necesita exponerse en scope del `createApiRoutes()`. Refactorizar si es necesario: pasar config como parametro a `createApiRoutes(config)` o usar una variable de modulo.

---

## Tarea 4: Console UI — Campanas (lista y formulario)

**Archivo**: `src/modules/marketing-data/templates.ts`

### 4a. Labels nuevos
Agregar a ambos idiomas en el objeto `labels`:

```typescript
// es:
col_origin: 'Origen',
origin_manual: 'Manual',
origin_auto: 'Auto UTM',
col_utm_keys: 'UTM Keys',
form_utm_keys: 'UTM Campaign Keys (separados por coma)',
col_match_source: 'Fuente',
settings_title: 'Configuracion de deteccion',
settings_utm_enabled: 'Deteccion UTM habilitada',
settings_keyword_enabled: 'Deteccion Keyword habilitada',
stats_by_source: 'Por fuente de deteccion',
stats_by_utm: 'Por fuente UTM',
stats_first_touch: 'First-touch',
stats_last_touch: 'Last-touch (conversion)',

// en:
col_origin: 'Origin',
origin_manual: 'Manual',
origin_auto: 'Auto UTM',
col_utm_keys: 'UTM Keys',
form_utm_keys: 'UTM Campaign Keys (comma separated)',
col_match_source: 'Source',
settings_title: 'Detection settings',
settings_utm_enabled: 'UTM detection enabled',
settings_keyword_enabled: 'Keyword detection enabled',
stats_by_source: 'By detection source',
stats_by_utm: 'By UTM source',
stats_first_touch: 'First-touch',
stats_last_touch: 'Last-touch (conversion)',
```

### 4b. Badge de origin en la lista de campanas

En la funcion `renderCampaigns()`, despues de la linea del nombre (aprox linea 312 del template actual), agregar badge de origin:

```javascript
// Despues de la linea del nombre (#visibleId + name)
const originBadge = c.origin === 'auto_utm'
  ? '<span style="display:inline-block;margin-left:6px;padding:1px 6px;border-radius:8px;font-size:10px;background:var(--warning-dim,#fef3c7);color:var(--warning,#d97706);border:1px solid var(--warning,#d97706)33;">Auto UTM</span>'
  : '';
```

Insertar `originBadge` en el HTML del nombre de campana:
```javascript
'<div style="font-size:15px;font-weight:500;color:var(--on-surface);">#' + c.visibleId + ' ' + c.name + originBadge + '</div>'
```

### 4c. Mostrar utm_keys en la lista
Agregar debajo de la linea de Keyword:

```javascript
const utmKeysLine = c.utmKeys && c.utmKeys.length > 0
  ? '<div style="font-size:12px;color:var(--on-surface-dim);margin-top:2px;">UTM Keys: ' +
    c.utmKeys.map(k => '<code style="background:var(--surface-variant);padding:1px 6px;border-radius:3px;">' + k + '</code>').join(' ') +
    '</div>'
  : '';
```

### 4d. Campo utm_keys en el modal de edicion/creacion

Agregar un campo input DESPUES del campo de prompt context (linea ~197 del HTML):

```html
<div>
  <label style="font-size:12px;color:var(--on-surface-dim);display:block;margin-bottom:4px;">${esc(l.form_utm_keys!)}</label>
  <input type="text" id="md-form-utm-keys" placeholder="black-friday,bf-2024,summer-sale" style="width:100%;padding:8px 12px;border-radius:6px;border:1px solid var(--border-color);background:var(--surface-variant);color:var(--on-surface);font-size:14px;box-sizing:border-box;">
</div>
```

### 4e. Actualizar submit del form

En el event listener de `md-campaign-form` submit (linea ~400), agregar al body:

```javascript
const utmKeysRaw = document.getElementById('md-form-utm-keys').value;
body.utmKeys = utmKeysRaw ? utmKeysRaw.split(',').map(s => s.trim()).filter(Boolean) : [];
```

### 4f. Actualizar openEditCampaign

En `openEditCampaign()` (linea ~362), popular el campo:
```javascript
document.getElementById('md-form-utm-keys').value = (c.utmKeys || []).join(', ');
```

Y en `openNewCampaign()`:
```javascript
document.getElementById('md-form-utm-keys').value = '';
```

### 4g. Hacer keyword NO required

En el HTML del form, cambiar el campo keyword de `required` a opcional:
```html
<!-- De: -->
<input type="text" id="md-form-keyword" required ...>
<!-- A: -->
<input type="text" id="md-form-keyword" ...>
```

Razon: las campanas auto-creadas por UTM no tienen keyword, y el admin podria querer crear una campana solo con utm_keys.

En el handler POST del API (manifest.ts), relajar la validacion:
```typescript
// De:
if (!body.name || !body.keyword) {
  jsonResponse(res, 400, { error: 'Missing name or keyword' }); return
}
// A:
if (!body.name) {
  jsonResponse(res, 400, { error: 'Missing name' }); return
}
```

---

## Tarea 5: Console UI — Tab de estadisticas expandido

**Archivo**: `src/modules/marketing-data/templates.ts`

### 5a. Reescribir `loadStats()`

Cambiar para llamar al nuevo endpoint:
```javascript
async function loadStats() {
  const data = await api('campaign-detailed-stats');
  const stats = data.stats || [];
  renderStats(stats);
}
```

### 5b. Reescribir `renderStats()`

Mantener la seccion de summary cards existente (total entries, conversions, rate) y agregar:

**Seccion 1 — Tabla por campana (existente, mejorada)**
Agregar columna "First-touch" a la tabla existente:
```
| Campana | Entradas (LT) | First-touch | Conversiones | Tasa |
```

Donde "Entradas (LT)" es last-touch (como hoy) y "First-touch" es el nuevo campo.

**Seccion 2 — Breakdown por fuente de deteccion**
Tabla colapsable por campana que muestra `sourceBreakdown`:
```
| Campana | Keyword | URL UTM | Webhook | Webhook UTM |
```

Implementacion: despues de la tabla principal, agregar una tabla expandible.
Para mantener la UI simple, mostrar como una segunda tabla separada con titulo "Por fuente de deteccion".

```javascript
// Generar tabla de source breakdown
html += '<h4 style="margin:20px 0 12px;font-size:14px;color:var(--on-surface);">' + l.stats_by_source + '</h4>';
html += '<div class="panel" style="border-radius:8px;border:1px solid var(--border-color);overflow:hidden;">';
html += '<table style="width:100%;border-collapse:collapse;font-size:13px;">';
html += '<thead><tr style="background:var(--surface-variant);">' +
  '<th style="padding:10px 12px;text-align:left;">Campana</th>' +
  '<th style="padding:10px 12px;text-align:right;">Keyword</th>' +
  '<th style="padding:10px 12px;text-align:right;">URL UTM</th>' +
  '<th style="padding:10px 12px;text-align:right;">Webhook</th>' +
  '<th style="padding:10px 12px;text-align:right;">Webhook UTM</th>' +
'</tr></thead><tbody>';

stats.forEach(s => {
  if (!s.campaignId) return; // skip "Sin campana" para esta vista
  const getCount = (source) => {
    const found = (s.sourceBreakdown || []).find(b => b.matchSource === source);
    return found ? found.entries : 0;
  };
  html += '<tr style="border-top:1px solid var(--border-color);">' +
    '<td style="padding:8px 12px;">#' + (s.visibleId||'') + ' ' + s.name + '</td>' +
    '<td style="padding:8px 12px;text-align:right;">' + getCount('keyword') + '</td>' +
    '<td style="padding:8px 12px;text-align:right;">' + getCount('url_utm') + '</td>' +
    '<td style="padding:8px 12px;text-align:right;">' + getCount('webhook') + '</td>' +
    '<td style="padding:8px 12px;text-align:right;">' + getCount('webhook_utm') + '</td>' +
  '</tr>';
});
html += '</tbody></table></div>';
```

**Seccion 3 — Top UTM sources (global)**
Tabla simple que muestra las top fuentes UTM:

```javascript
// Cargar UTM breakdown global
const utmData = await api('utm-breakdown');
const utmBreakdown = utmData.breakdown || [];

if (utmBreakdown.length > 0) {
  html += '<h4 style="margin:20px 0 12px;font-size:14px;color:var(--on-surface);">' + l.stats_by_utm + '</h4>';
  html += '<div class="panel" style="border-radius:8px;border:1px solid var(--border-color);overflow:hidden;">';
  html += '<table style="width:100%;border-collapse:collapse;font-size:13px;">';
  html += '<thead><tr style="background:var(--surface-variant);">' +
    '<th style="padding:10px 12px;text-align:left;">utm_source</th>' +
    '<th style="padding:10px 12px;text-align:left;">utm_medium</th>' +
    '<th style="padding:10px 12px;text-align:right;">Entradas</th>' +
    '<th style="padding:10px 12px;text-align:right;">Conversiones</th>' +
    '<th style="padding:10px 12px;text-align:right;">Tasa</th>' +
  '</tr></thead><tbody>';
  utmBreakdown.forEach(u => {
    const rate = u.entries > 0 ? ((u.conversions / u.entries) * 100).toFixed(1) : '0.0';
    html += '<tr style="border-top:1px solid var(--border-color);">' +
      '<td style="padding:8px 12px;">' + (u.utmSource || 'unknown') + '</td>' +
      '<td style="padding:8px 12px;">' + (u.utmMedium || 'unknown') + '</td>' +
      '<td style="padding:8px 12px;text-align:right;">' + u.entries + '</td>' +
      '<td style="padding:8px 12px;text-align:right;">' + u.conversions + '</td>' +
      '<td style="padding:8px 12px;text-align:right;">' + rate + '%</td>' +
    '</tr>';
  });
  html += '</tbody></table></div>';
}
```

**NOTA**: `loadStats()` ahora hace 2 fetches (detailed-stats + utm-breakdown). Usar `Promise.all()`:
```javascript
async function loadStats() {
  const [statsData, utmData] = await Promise.all([
    api('campaign-detailed-stats'),
    api('utm-breakdown'),
  ]);
  renderStats(statsData.stats || [], utmData.breakdown || []);
}
```

---

## Tarea 6: Console UI — Settings panel

**Archivo**: `src/modules/marketing-data/templates.ts`

### 6a. Agregar seccion de settings al tab de campanas

Al inicio del tab de campanas (antes de la lista), agregar un panel colapsable de configuracion:

```html
<div style="margin-bottom:16px;padding:12px 16px;border-radius:8px;border:1px solid var(--border-color);background:var(--surface-variant);">
  <div style="font-size:13px;font-weight:500;color:var(--on-surface);margin-bottom:8px;">${esc(l.settings_title!)}</div>
  <div style="display:flex;gap:24px;flex-wrap:wrap;">
    <label style="display:flex;align-items:center;gap:8px;font-size:13px;color:var(--on-surface);cursor:pointer;">
      <input type="checkbox" id="md-toggle-utm" checked style="width:16px;height:16px;">
      ${esc(l.settings_utm_enabled!)}
    </label>
    <label style="display:flex;align-items:center;gap:8px;font-size:13px;color:var(--on-surface);cursor:pointer;">
      <input type="checkbox" id="md-toggle-keyword" checked style="width:16px;height:16px;">
      ${esc(l.settings_keyword_enabled!)}
    </label>
  </div>
</div>
```

### 6b. Cargar estado actual de toggles

En el script, al cargar campanas, tambien cargar config:
```javascript
async function loadConfig() {
  const data = await api('config');
  document.getElementById('md-toggle-utm').checked = data.utmMatchEnabled !== false;
  document.getElementById('md-toggle-keyword').checked = data.keywordMatchEnabled !== false;
}
```

Llamar `loadConfig()` en el initial load.

### 6c. Nota sobre persistencia de toggles

Los toggles son de **solo lectura en la consola** por ahora (reflejan el valor de las env vars `CAMPAIGN_UTM_MATCH_ENABLED` y `CAMPAIGN_KEYWORD_MATCH_ENABLED`). Para cambiarlos, el admin modifica las env vars y reinicia.

Si se quiere hacerlos editables en runtime (sin reinicio), se necesitaria persistir en `config_store` y leerlos en runtime. Esto es una mejora futura — por ahora los checkboxes son readonly:

```javascript
document.getElementById('md-toggle-utm').disabled = true;
document.getElementById('md-toggle-keyword').disabled = true;
```

Con un tooltip: "Configurable via variables de entorno CAMPAIGN_UTM_MATCH_ENABLED / CAMPAIGN_KEYWORD_MATCH_ENABLED".

**Razon**: Mantener la implementacion simple. La infraestructura de config_store para runtime toggles es un feature aparte que no deberia bloquear este plan.

---

## Tarea 7: Actualizar API POST/PUT campaign

**Archivo**: `src/modules/marketing-data/manifest.ts`

### 7a. POST campaign — aceptar utmKeys, hacer keyword opcional

```typescript
// En el handler POST:
const body = await parseBody<{
  name: string
  keyword?: string              // ahora opcional
  matchThreshold?: number
  matchMaxRounds?: number
  allowedChannels?: string[]
  promptContext?: string
  utmData?: Record<string, string>
  utmKeys?: string[]            // NUEVO
  tagIds?: string[]
}>(req)
if (!body.name) {
  jsonResponse(res, 400, { error: 'Missing name' }); return
}
```

### 7b. PUT campaign — aceptar utmKeys
```typescript
// Agregar utmKeys al tipo de body del PUT:
utmKeys?: string[]
```

---

## Tarea 8: Documentacion

### 8a. CLAUDE.md del modulo
**Archivo**: `src/modules/marketing-data/CLAUDE.md`

Reescribir para reflejar la nueva funcionalidad. Contenido actualizado:

```markdown
# Marketing Data — Gestion de campanas de marketing

Modulo de campanas con deteccion dual (UTM + keyword), auto-creacion desde UTMs, metricas desglosadas por fuente, y tags de categorización.

## Archivos
- `manifest.ts` — lifecycle, servicios, API routes, console SSR, config toggles
- `campaign-types.ts` — CampaignRecord, UtmParams, CampaignMatchResult, stats types
- `campaign-queries.ts` — CRUD campanas, tags, contact-campaign history, stats desglosadas
- `campaign-matcher.ts` — keyword fuzzy match (fuse.js) + UTM campaign lookup/auto-create
- `utm-parser.ts` — extraccion de UTMs de URLs en texto, normalizacion
- `templates.ts` — SSR HTML: tabs (campanas, tags, stats), modals, settings, JS inline

## Deteccion de campanas (prioridad)
1. **UTM match** (si habilitado): parsea URLs en mensaje o utm en webhook → busca/auto-crea campana por utm_campaign
2. **Keyword match** (si habilitado, solo si UTM no matcheo): fuzzy match fuse.js contra texto
3. **UTM SIEMPRE GANA sobre keyword**

## Auto-creacion
Cuando llega un utm_campaign sin campana existente, se auto-crea con origin='auto_utm'. Badge visible en consola.

## Config toggles (env vars)
- `CAMPAIGN_UTM_MATCH_ENABLED` (default true)
- `CAMPAIGN_KEYWORD_MATCH_ENABLED` (default true)

## Servicios registrados
- `marketing-data:campaign-queries` — instancia de CampaignQueries
- `marketing-data:match-campaign` — keyword match (sync)
- `marketing-data:match-campaign-utm` — UTM match (async, con auto-create)
- `marketing-data:reload-campaigns` — recarga indice del matcher

## Tablas
- `campaigns` — campanas con keyword, utm_keys[], origin, threshold, channels
- `campaign_tags` — tags de plataforma y fuente
- `campaign_tag_assignments` — join table campana-tag
- `contact_campaigns` — historial con match_source y utm_data JSONB

## Metricas
- Entries + conversiones por campana (last-touch attribution)
- First-touch attribution
- Breakdown por match_source (keyword/url_utm/webhook/webhook_utm)
- Breakdown por UTM source/medium (global y por campana)

## Trampas
- keyword es OPCIONAL en campanas (las auto-creadas no tienen)
- utm_keys[] es un array: una campana puede tener multiples utm_campaign values
- Auto-creacion usa ON CONFLICT para evitar race conditions
- Toggles son readonly en consola (env vars, requieren reinicio)
```

### 8b. CLAUDE.md raiz
**Archivo**: `/home/user/LUNA/CLAUDE.md`

Verificar que la entrada de `src/modules/marketing-data/CLAUDE.md` en la lista de "Modulos documentados" existe. Si ya existe, no modificar (la descripcion corta es suficiente).

---

## Tarea 9: Compilacion y verificacion

1. Correr `npx tsc --noEmit` (dentro del contenedor Docker si aplica)
2. Verificar que no hay errores de tipo
3. Verificar que la consola renderiza correctamente los nuevos campos
4. Verificar que el endpoint `campaign-detailed-stats` retorna datos con breakdown
5. Verificar que `utm-breakdown` retorna datos (puede estar vacio si no hay UTMs aun)

---

## Archivos modificados (resumen)

| Archivo | Accion |
|---------|--------|
| `src/modules/marketing-data/campaign-types.ts` | SourceBreakdown, UtmBreakdown, CampaignDetailedStats |
| `src/modules/marketing-data/campaign-queries.ts` | getCampaignDetailedStats(), getGlobalUtmBreakdown() |
| `src/modules/marketing-data/manifest.ts` | API routes: campaign-detailed-stats, utm-breakdown, config; POST/PUT acepta utmKeys; keyword opcional |
| `src/modules/marketing-data/templates.ts` | Labels, origin badge, utm_keys field/display, stats expandidos, settings panel |
| `src/modules/marketing-data/CLAUDE.md` | Reescrito con nueva funcionalidad |

## Riesgos
1. **Performance de stats queries**: 5 queries SQL para detailed stats. Mitigacion: son queries simples con indices existentes. Para tablas con <100K filas, <50ms total.
2. **UI complexity**: El tab de stats crece significativamente. Mitigacion: secciones separadas con titulos claros, sin colapsables complejos.
3. **Toggles readonly**: El admin no puede cambiar toggles sin reinicio. Es una limitacion aceptable para v1. Mejora futura: persistir en config_store.
