# Plan 1 — Bugfixes críticos + robustez

## Objetivo
Arreglar los 4 bugs que impiden que el sistema proactivo y scheduled-tasks funcionen en producción. Sin esto, nada más importa.

## Contexto
- El cron dummy `0 0 31 2 *` (31 de febrero) crashea el módulo `scheduled-tasks` al arrancar porque `cron-parser` lo rechaza.
- `bootstrap.ts` solo crea directorios, no copia archivos template como `proactive.json`. Instancias con volume mount pierden el archivo.
- `scheduled-tasks/scheduler.ts` carga TODAS las tareas habilitadas e intenta registrarlas como repeatable jobs, incluyendo las de tipo `manual` y `event` que no necesitan cron.
- El jobId `scheduled:{taskId}` usa `:` en repeatable jobs (línea 116 y 153 de scheduler.ts). Aunque FIX-03 ya arregló los delayed jobs, los repeatable siguen con `:`.

---

## Cambio 1: Reemplazar cron dummy inválido

### Qué hacer
Reemplazar `'0 0 31 2 *'` por `'0 0 1 1 *'` (1 de enero a medianoche — válido, prácticamente nunca dispara en producción porque las tareas manual/event no se registran como repeatable gracias al Cambio 3).

### Archivos a modificar

**`src/modules/scheduled-tasks/api-routes.ts` línea 132:**
```typescript
// ANTES
if (!input.cron) input.cron = '0 0 31 2 *' // never fires via cron

// DESPUÉS
if (!input.cron) input.cron = '0 0 1 1 *' // placeholder for non-cron tasks (valid, never scheduled)
```

**`src/modules/medilink/follow-up-scheduler.ts` línea 161:**
```typescript
// ANTES
cron: '0 0 31 2 *', // dummy cron (never fires — triggered by delayed job)

// DESPUÉS
cron: '0 0 1 1 *', // placeholder (never scheduled as repeatable — triggered by delayed job)
```

**`src/modules/google-apps/calendar-followups.ts` líneas 284 y 439:**
```typescript
// ANTES (ambas líneas)
cron: '0 0 31 2 *', // dummy cron (31 de febrero — nunca ocurre)

// DESPUÉS (ambas líneas)
cron: '0 0 1 1 *', // placeholder (never scheduled as repeatable — triggered by delayed job)
```

**`src/modules/scheduled-tasks/CLAUDE.md` línea 78:**
```markdown
// ANTES
- Tareas de evento usan cron dummy `0 0 31 2 *` (nunca dispara) para satisfacer schema

// DESPUÉS
- Tareas de evento/manual usan cron placeholder `0 0 1 1 *` para satisfacer schema. No se registran como repeatable en BullMQ.
```

---

## Cambio 2: bootstrap.ts copia archivos template faltantes

### Qué hacer
Agregar a `bootstrap.ts` una lista de archivos obligatorios que deben existir en `instance/`. Si faltan, copiarlos desde el template base del repo.

### Archivo: `src/kernel/bootstrap.ts`

Agregar después de `REQUIRED_DIRS` (línea 17):

```typescript
/**
 * Files that must exist in instance/. If missing (e.g. volume mount overwrote
 * the directory), copy them from the repo template.
 */
const REQUIRED_FILES: Array<{ target: string; source: string }> = [
  { target: 'instance/proactive.json', source: 'instance/proactive.json' },
]
```

Agregar nueva función después de `ensureInstanceDirs`:

```typescript
/**
 * Copy required instance files from the repo template if they don't exist.
 * Handles the case where a Docker volume mount overwrites instance/ without
 * including template files like proactive.json.
 */
export async function ensureInstanceFiles(): Promise<void> {
  for (const { target, source } of REQUIRED_FILES) {
    const targetPath = path.resolve(target)
    const sourcePath = path.resolve(source)

    try {
      await fs.access(targetPath)
      // File exists, skip
    } catch {
      // File missing — try to copy from source
      try {
        await fs.access(sourcePath)
        await fs.copyFile(sourcePath, targetPath)
        logger.info({ file: target }, 'Copied missing instance file from template')
      } catch {
        logger.warn({ file: target }, 'Instance file missing and no template source found')
      }
    }
  }
}
```

### Archivo: `src/index.ts` (o donde se llama `ensureInstanceDirs`)

Buscar la llamada a `ensureInstanceDirs()` y agregar `ensureInstanceFiles()` justo después:

```typescript
import { ensureInstanceDirs, ensureInstanceFiles } from './kernel/bootstrap.js'
// ...
await ensureInstanceDirs()
await ensureInstanceFiles()
```

**NOTA**: Verificar en `src/index.ts` dónde exactamente se llama a `ensureInstanceDirs()` y agregar `ensureInstanceFiles()` inmediatamente después, antes del setup wizard y antes de cargar módulos.

---

## Cambio 3: scheduled-tasks no registra non-cron tasks como repeatable

### Qué hacer
En `scheduler.ts`, la función `scheduleTask()` debe verificar `trigger_type` antes de registrar un repeatable job. Si no es `'cron'`, no llamar a `queue.add()` con repeat.

### Archivo: `src/modules/scheduled-tasks/scheduler.ts`

**Función `scheduleTask()` (línea 113-145):**

Agregar guard después de `if (!task.enabled) return` (línea 121):

```typescript
export async function scheduleTask(task: ScheduledTask): Promise<void> {
  if (!queue) return

  const jobId = `scheduled-${task.id}`  // FIX: replace : with - (Cambio 4)

  // Remove existing repeatable if any
  await unscheduleTask(task.id)

  if (!task.enabled) return

  // Only cron-triggered tasks need repeatable BullMQ jobs.
  // Manual and event tasks are triggered via API/hooks or delayed jobs.
  if (task.trigger_type !== 'cron') return

  // FIX-04: Wrap scheduling in try/catch so an invalid cron expression
  // does not crash the entire module — log ERROR and skip the task.
  let repeatJob
  try {
    repeatJob = await queue.add(
      task.name,
      { taskId: task.id, taskName: task.name },
      {
        repeat: { pattern: task.cron, ...(agentTimezone ? { tz: agentTimezone } : {}) },
        jobId,
      },
    )
  } catch (err) {
    logger.error({ err, taskId: task.id, cron: task.cron }, 'Invalid cron expression — task skipped, fix the cron pattern')
    return
  }

  if (repeatJob.repeatJobKey) {
    repeatJobKeys.push(repeatJob.repeatJobKey)
  }

  logger.info({ taskId: task.id, cron: task.cron }, 'Task scheduled')
}
```

**Función `unscheduleTask()` (línea 147-160):**

Actualizar la comparación de jobId:

```typescript
export async function unscheduleTask(taskId: string): Promise<void> {
  if (!queue) return

  const repeatables = await queue.getRepeatableJobs()
  for (const r of repeatables) {
    // Match both old format (scheduled:) and new (scheduled-)
    if (r.id === `scheduled-${taskId}` || r.id === `scheduled:${taskId}`) {
      await queue.removeRepeatableByKey(r.key)
      const idx = repeatJobKeys.indexOf(r.key)
      if (idx >= 0) repeatJobKeys.splice(idx, 1)
      break
    }
  }
}
```

**También actualizar `startScheduler()` (líneas 84-91):**

El loop que carga tareas al arrancar debe filtrar por trigger_type:

```typescript
// Load and schedule all enabled cron tasks
const tasks = await store.listTasks(db)
for (const task of tasks) {
  if (task.enabled && task.trigger_type === 'cron') {
    await scheduleTask(task)
  }
}

logger.info({ count: tasks.filter(t => t.enabled && t.trigger_type === 'cron').length }, 'Scheduler started')
```

---

## Cambio 4: Reemplazar `:` por `-` en jobIds de repeatable jobs

### Qué hacer
En `scheduler.ts` línea 116, el jobId para repeatable jobs usa `scheduled:${task.id}`. BullMQ rechaza `:` en custom jobIds. Aunque el try/catch de FIX-04 previene el crash, el job nunca se registra correctamente.

### Archivo: `src/modules/scheduled-tasks/scheduler.ts` línea 116

```typescript
// ANTES
const jobId = `scheduled:${task.id}`

// DESPUÉS
const jobId = `scheduled-${task.id}`
```

**NOTA**: Este cambio ya está reflejado en el Cambio 3 arriba. Solo asegurarse de que la línea sea `scheduled-${task.id}`.

---

## Documentación a actualizar

### `src/modules/scheduled-tasks/CLAUDE.md`
Actualizar secciones:
- **Cron validation (FIX-04)**: mencionar que `scheduleTask` ahora filtra por `trigger_type === 'cron'`
- **JobId uniqueness (FIX-03)**: mencionar que repeatable jobs también usan `-` en vez de `:`
- **Trampas**: actualizar entrada sobre cron dummy de `0 0 31 2 *` a `0 0 1 1 *`
- Agregar trampa: "Solo tareas `trigger_type='cron'` se registran como repeatable en BullMQ"

### `src/engine/proactive/CLAUDE.md`
No requiere cambios para este plan.

### `src/kernel/bootstrap.ts` (inline docs)
Ya incluidos en el código del Cambio 2.

---

## Verificación

1. **Compilar**: `docker run --rm -v /docker/luna-repo:/app -w /app node:22-alpine npx tsc --noEmit` — sin errores
2. **Buscar remanentes**: `grep -r "0 0 31 2" src/` — debe retornar 0 resultados
3. **Buscar colons en jobIds**: `grep -r "scheduled:" src/modules/scheduled-tasks/scheduler.ts` — solo debe aparecer en el `unscheduleTask` legacy match, no en construcción de nuevos IDs
4. **Verificar bootstrap exports**: confirmar que `ensureInstanceFiles` se exporta e importa correctamente
5. **Verificar trigger_type filter**: en `startScheduler()`, solo tareas con `trigger_type === 'cron'` se pasan a `scheduleTask()`

---

## Resumen de archivos modificados

| Archivo | Cambio |
|---------|--------|
| `src/modules/scheduled-tasks/api-routes.ts` | Cron dummy → `0 0 1 1 *` |
| `src/modules/medilink/follow-up-scheduler.ts` | Cron dummy → `0 0 1 1 *` |
| `src/modules/google-apps/calendar-followups.ts` | Cron dummy → `0 0 1 1 *` (2 ocurrencias) |
| `src/modules/scheduled-tasks/scheduler.ts` | Guard `trigger_type !== 'cron'`, jobId `:` → `-`, startup filter |
| `src/kernel/bootstrap.ts` | `REQUIRED_FILES` + `ensureInstanceFiles()` |
| `src/index.ts` | Llamar `ensureInstanceFiles()` después de `ensureInstanceDirs()` |
| `src/modules/scheduled-tasks/CLAUDE.md` | Actualizar documentación |
