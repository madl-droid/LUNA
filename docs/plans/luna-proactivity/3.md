# Plan 3 — Follow-up intensity per-contact

## Objetivo
Permitir que cada contacto tenga un nivel de intensidad de seguimiento configurable, en vez de un valor global para todos.

## Contexto
- Hoy `follow-up.ts` usa `proactive.json` → `follow_up.inactivity_hours: 4` y `max_attempts: 3` para TODOS los leads igual.
- Smart cooldown adapta por outcome pero no por preferencia o contexto del lead.
- La columna `follow_up_intensity` se crea en la migración del Plan 2 (en `agent_contacts`).
- Luna o el operador pueden configurar la intensidad durante la conversación o desde la consola.

## Dependencia
- **Requiere Plan 1 completado** (sistema proactivo funcional).
- **La migración SQL se crea en Plan 2** (columna `follow_up_intensity` en `agent_contacts`). Si Plan 3 se ejecuta antes de Plan 2, crear la migración aquí en su lugar.

---

## Niveles de intensidad

| Nivel | inactivity_hours | max_attempts | Caso de uso |
|-------|-----------------|--------------|-------------|
| `aggressive` | 2 | 5 | Lead caliente, urgente, interesado |
| `normal` | 4 | 3 | Default, lead estándar |
| `gentle` | 12 | 2 | Lead tibio, no presionar |
| `minimal` | 24 | 1 | Lead frío, solo 1 intento |

---

## Cambio 1: Definir constantes de intensidad

### Archivo: Crear `src/engine/proactive/intensity.ts`

```typescript
// LUNA Engine — Follow-up intensity levels
// Per-contact follow-up configuration. Stored in agent_contacts.follow_up_intensity.

export type FollowUpIntensity = 'aggressive' | 'normal' | 'gentle' | 'minimal'

export interface IntensityConfig {
  inactivityHours: number
  maxAttempts: number
}

export const INTENSITY_LEVELS: Record<FollowUpIntensity, IntensityConfig> = {
  aggressive: { inactivityHours: 2, maxAttempts: 5 },
  normal:     { inactivityHours: 4, maxAttempts: 3 },
  gentle:     { inactivityHours: 12, maxAttempts: 2 },
  minimal:    { inactivityHours: 24, maxAttempts: 1 },
}

export const DEFAULT_INTENSITY: FollowUpIntensity = 'normal'

/**
 * Resolve intensity config for a contact.
 * Falls back to global config values if intensity is unknown.
 */
export function resolveIntensity(
  intensity: string | null | undefined,
  globalInactivityHours: number,
  globalMaxAttempts: number,
): IntensityConfig {
  if (intensity && intensity in INTENSITY_LEVELS) {
    return INTENSITY_LEVELS[intensity as FollowUpIntensity]
  }
  return { inactivityHours: globalInactivityHours, maxAttempts: globalMaxAttempts }
}
```

---

## Cambio 2: follow-up scanner respeta intensidad per-contact

### Qué hacer
Modificar `follow-up.ts` para que lea `follow_up_intensity` de cada lead y aplique los parámetros correspondientes en vez de los globales.

### Archivo: `src/engine/proactive/jobs/follow-up.ts`

**Agregar import:**
```typescript
import { resolveIntensity } from '../intensity.js'
```

**Modificar el query principal** (líneas 28-42) para incluir `follow_up_intensity`:

```sql
SELECT ac.contact_id, ac.follow_up_count, ac.follow_up_intensity,
       c.display_name,
       cc.channel_identifier, cc.channel_type
FROM agent_contacts ac
JOIN contacts c ON c.id = ac.contact_id
JOIN contact_channels cc ON cc.contact_id = ac.contact_id AND cc.is_primary = true
WHERE ac.lead_status IN ('new', 'qualifying')
  AND ac.updated_at > now() - interval '7 days'
ORDER BY ac.updated_at ASC
LIMIT 40
```

**NOTA IMPORTANTE**: Eliminamos los filtros `follow_up_count < $1` y `updated_at < now() - interval '1 hour' * $2` del query SQL porque ahora son per-contact. Los filtramos en código después:

```typescript
// Filter candidates by their individual intensity config
const candidates = result.rows.filter(row => {
  const intensity = resolveIntensity(
    row.follow_up_intensity,
    config.follow_up.inactivity_hours,
    config.follow_up.max_attempts,
  )

  // Check if contact has been inactive long enough for their intensity level
  const lastActivity = new Date(row.updated_at)
  const hoursSinceActivity = (Date.now() - lastActivity.getTime()) / (1000 * 60 * 60)
  if (hoursSinceActivity < intensity.inactivityHours) return false

  // Check if under max attempts for their intensity level
  if ((row.follow_up_count ?? 0) >= intensity.maxAttempts) return false

  return true
})
```

**NOTA**: El query necesita incluir `ac.updated_at` en el SELECT para el filtro en código. Agregar si no está.

**Modificar la lógica de max attempts** (líneas 70-82):

```typescript
// Check if max attempts reached on primary channel (per-contact intensity)
const intensity = resolveIntensity(
  row.follow_up_intensity,
  config.follow_up.inactivity_hours,
  config.follow_up.max_attempts,
)
const newCount = (row.follow_up_count ?? 0) + 1
if (newCount >= intensity.maxAttempts) {
  if (config.follow_up.cross_channel) {
    const crossResult = await tryCrossChannelFollowUp(ctx, row.contact_id, row.display_name, row.channel_type as ChannelName)
    if (!crossResult) {
      await transitionToCold(ctx, row.contact_id)
    }
  } else {
    await transitionToCold(ctx, row.contact_id)
  }
}
```

---

## Cambio 3: Tool para que Luna configure intensidad

### Qué hacer
Registrar un tool `set_follow_up_intensity` que Luna pueda usar durante la conversación para ajustar la intensidad de seguimiento de un contacto.

### Archivo: Crear `src/engine/proactive/tools/set-intensity.ts`

```typescript
// LUNA Engine — set_follow_up_intensity tool
// Lets the agent adjust follow-up intensity for a contact based on conversation context.

import pino from 'pino'
import type { Registry } from '../../../kernel/registry.js'
import { INTENSITY_LEVELS, type FollowUpIntensity } from '../intensity.js'

const logger = pino({ name: 'engine:tool:set-intensity' })

interface ToolRegistry {
  registerTool(toolDef: {
    definition: {
      name: string
      displayName: string
      description: string
      category: string
      sourceModule: string
      parameters: {
        type: 'object'
        properties: Record<string, { type: string; description: string; enum?: string[] }>
        required?: string[]
      }
    }
    handler: (input: Record<string, unknown>, ctx: {
      contactId?: string; correlationId: string; db: import('pg').Pool
    }) => Promise<{ success: boolean; data?: unknown; error?: string }>
  }): Promise<void>
}

export async function registerSetIntensityTool(registry: Registry): Promise<void> {
  const toolRegistry = registry.getOptional<ToolRegistry>('tools:registry')
  if (!toolRegistry) return

  const levels = Object.entries(INTENSITY_LEVELS)
    .map(([k, v]) => `${k}: follow-up every ${v.inactivityHours}h, max ${v.maxAttempts} attempts`)
    .join('; ')

  await toolRegistry.registerTool({
    definition: {
      name: 'set_follow_up_intensity',
      displayName: 'Configurar Intensidad de Seguimiento',
      description: `Set follow-up intensity for this contact. Levels: ${levels}. Use based on contact's engagement and interest level.`,
      category: 'internal',
      sourceModule: 'engine',
      parameters: {
        type: 'object',
        properties: {
          intensity: {
            type: 'string',
            enum: Object.keys(INTENSITY_LEVELS),
            description: 'Follow-up intensity level. aggressive=hot lead, normal=standard, gentle=don\'t push, minimal=almost no follow-up.',
          },
          reason: {
            type: 'string',
            description: 'Why this intensity was chosen (for audit trail).',
          },
        },
        required: ['intensity'],
      },
    },
    handler: async (input, ctx) => {
      if (!ctx.contactId) {
        return { success: false, error: 'No contact_id in execution context' }
      }

      const intensity = String(input.intensity) as FollowUpIntensity
      if (!(intensity in INTENSITY_LEVELS)) {
        return { success: false, error: `Invalid intensity: ${intensity}. Use: ${Object.keys(INTENSITY_LEVELS).join(', ')}` }
      }

      try {
        await ctx.db.query(
          `UPDATE agent_contacts SET follow_up_intensity = $1, updated_at = now()
           WHERE contact_id = $2`,
          [intensity, ctx.contactId],
        )

        const config = INTENSITY_LEVELS[intensity]
        logger.info({
          contactId: ctx.contactId,
          intensity,
          reason: input.reason,
        }, 'Follow-up intensity updated')

        return {
          success: true,
          data: {
            intensity,
            inactivity_hours: config.inactivityHours,
            max_attempts: config.maxAttempts,
            message: `Seguimiento configurado: ${intensity} (cada ${config.inactivityHours}h, máx ${config.maxAttempts} intentos)`,
          },
        }
      } catch (err) {
        logger.error({ err, contactId: ctx.contactId }, 'Failed to set follow-up intensity')
        return { success: false, error: 'Failed to update follow-up intensity' }
      }
    },
  })

  logger.info('set_follow_up_intensity tool registered')
}
```

---

## Cambio 4: Registrar el tool en el engine

### Qué hacer
El tool se registra donde se registran los demás tools proactivos. Buscar dónde se llama `registerCreateCommitmentTool()` y `registerUpdateCommitmentTool()` y agregar el nuevo tool.

### Archivo: Buscar en `src/engine/` o `src/modules/engine/manifest.ts` donde se registran los proactive tools

Agregar:
```typescript
import { registerSetIntensityTool } from '../engine/proactive/tools/set-intensity.js'
// ...
await registerSetIntensityTool(registry)
```

**NOTA**: Verificar el archivo exacto. Buscar `registerCreateCommitmentTool` para encontrar dónde se hace el registro.

---

## Cambio 5: Inyectar intensidad actual en el contexto

### Archivo: `src/engine/prompts/context-builder.ts`

En la sección de lead status / qualification (alrededor de la línea 209), agregar la intensidad actual:

```typescript
// After the qualification section, add follow-up intensity if available
if (ctx.contact?.followUpIntensity && ctx.contact.followUpIntensity !== 'normal') {
  parts.push(`[Intensidad de seguimiento: ${ctx.contact.followUpIntensity}]`)
}
```

**NOTA**: Esto requiere que `followUpIntensity` esté disponible en el `contact` object del ContextBundle. Verificar si Phase 1 (intake.ts) ya carga `agent_contacts` con los campos necesarios. Si no, agregar al query de intake.

### Archivo: `src/engine/boundaries/intake.ts` (o donde se construye el contact en ContextBundle)

Buscar el query que carga `agent_contacts` y agregar `follow_up_intensity`:

```sql
SELECT ..., ac.follow_up_intensity, ...
FROM agent_contacts ac ...
```

Y mapear al contact object como `followUpIntensity`.

---

## Cambio 6: Migración SQL (solo si Plan 2 no se ejecuta primero)

Si Plan 2 ya creó la migración con `follow_up_intensity`, este paso se salta.

Si Plan 3 se ejecuta antes de Plan 2, crear:

### Archivo: `src/migrations/NNN_follow-up-intensity.sql`

```sql
-- Follow-up intensity per contact
-- Values: 'aggressive', 'normal', 'gentle', 'minimal'
ALTER TABLE agent_contacts ADD COLUMN IF NOT EXISTS follow_up_intensity TEXT NOT NULL DEFAULT 'normal';
```

---

## Documentación a actualizar

### `src/engine/proactive/CLAUDE.md`
Agregar sección:
```markdown
## Follow-up Intensity (`intensity.ts`)
- 4 niveles per-contact: aggressive (2h/5), normal (4h/3), gentle (12h/2), minimal (24h/1)
- Stored in `agent_contacts.follow_up_intensity`
- Tool `set_follow_up_intensity` permite al agente ajustar durante conversación
- `follow-up.ts` lee intensidad per-contact y aplica parámetros individuales
- Default: `normal` (compatible con config global existente)
```

### `CLAUDE.md` raíz
No requiere cambios.

---

## Verificación

1. **Compilar**: `npx tsc --noEmit` — sin errores
2. **Grep intensity**: `grep -r "follow_up_intensity" src/` — debe aparecer en migration, intensity.ts, follow-up.ts, set-intensity.ts, context-builder.ts, intake.ts
3. **Verificar default**: Leads existentes deben tener `follow_up_intensity = 'normal'` (default de la migración)
4. **Verificar backward compat**: Con intensidad `normal`, el comportamiento es idéntico al actual (4h, 3 attempts)

---

## Resumen de archivos creados/modificados

| Archivo | Cambio |
|---------|--------|
| `src/engine/proactive/intensity.ts` | **CREAR**: constantes y helper de intensidad |
| `src/engine/proactive/tools/set-intensity.ts` | **CREAR**: tool para configurar intensidad |
| `src/engine/proactive/jobs/follow-up.ts` | Leer intensity per-contact, filtrar en código |
| `src/engine/prompts/context-builder.ts` | Mostrar intensidad en contexto |
| `src/engine/boundaries/intake.ts` | Cargar follow_up_intensity en contact |
| Engine tool registration file | Registrar `set_follow_up_intensity` |
| `src/migrations/NNN_follow-up-intensity.sql` | **CREAR** (solo si Plan 2 no lo incluyó) |
| `src/engine/proactive/CLAUDE.md` | Documentar intensity system |
