# Plan 4 — Scheduled-tasks presets + Knowledge console field

## Objetivo
Reemplazar el input libre de cron en scheduled-tasks con presets fijos (dropdown), y mover `KNOWLEDGE_CONTACT_CATEGORY_MAP` a la consola como campo configurable.

## Dependencia
- **Requiere Plan 1 completado** (el cron dummy ya fue reemplazado y el módulo no crashea).
- **Independiente de Planes 2 y 3.**

---

## PARTE A: Cron presets en scheduled-tasks

### Presets definidos

| Grupo | Valor UI | Cron expression | Label ES | Label EN |
|-------|----------|----------------|----------|----------|
| Minutos | 5min | `*/5 * * * *` | Cada 5 minutos | Every 5 minutes |
| Minutos | 15min | `*/15 * * * *` | Cada 15 minutos | Every 15 minutes |
| Minutos | 30min | `*/30 * * * *` | Cada 30 minutos | Every 30 minutes |
| Horas | 1h | `0 * * * *` | Cada hora | Every hour |
| Horas | 2h | `0 */2 * * *` | Cada 2 horas | Every 2 hours |
| Horas | 6h | `0 */6 * * *` | Cada 6 horas | Every 6 hours |
| Horas | 12h | `0 */12 * * *` | Cada 12 horas | Every 12 hours |
| Días | 1d | `0 9 * * *` | Diario (9 AM) | Daily (9 AM) |
| Días | 3d | `0 9 */3 * *` | Cada 3 días (9 AM) | Every 3 days (9 AM) |
| Días | 5d | `0 9 * * 1-5` | Lun-Vie (9 AM) | Mon-Fri (9 AM) |
| Semanas | 1w | `0 9 * * 1` | Semanal (Lun 9 AM) | Weekly (Mon 9 AM) |
| Semanas | 2w | `0 9 1,15 * *` | Quincenal (1 y 15, 9 AM) | Bi-weekly (1st & 15th, 9 AM) |
| Mes | 1m | `0 9 1 * *` | Mensual (día 1, 9 AM) | Monthly (1st, 9 AM) |

---

### Cambio A1: Definir presets como constante

#### Archivo: `src/modules/scheduled-tasks/types.ts`

Agregar al final del archivo:

```typescript
/** Cron presets — no free-text cron allowed in UI */
export interface CronPreset {
  value: string  // unique key (e.g. '5min', '1h', '1d')
  cron: string   // actual cron expression
  label: { es: string; en: string }
  group: string  // for optgroup rendering
}

export const CRON_PRESETS: CronPreset[] = [
  // Minutes
  { value: '5min',  cron: '*/5 * * * *',   label: { es: 'Cada 5 minutos', en: 'Every 5 minutes' }, group: 'minutes' },
  { value: '15min', cron: '*/15 * * * *',  label: { es: 'Cada 15 minutos', en: 'Every 15 minutes' }, group: 'minutes' },
  { value: '30min', cron: '*/30 * * * *',  label: { es: 'Cada 30 minutos', en: 'Every 30 minutes' }, group: 'minutes' },
  // Hours
  { value: '1h',  cron: '0 * * * *',     label: { es: 'Cada hora', en: 'Every hour' }, group: 'hours' },
  { value: '2h',  cron: '0 */2 * * *',   label: { es: 'Cada 2 horas', en: 'Every 2 hours' }, group: 'hours' },
  { value: '6h',  cron: '0 */6 * * *',   label: { es: 'Cada 6 horas', en: 'Every 6 hours' }, group: 'hours' },
  { value: '12h', cron: '0 */12 * * *',  label: { es: 'Cada 12 horas', en: 'Every 12 hours' }, group: 'hours' },
  // Days
  { value: '1d',  cron: '0 9 * * *',     label: { es: 'Diario (9 AM)', en: 'Daily (9 AM)' }, group: 'days' },
  { value: '3d',  cron: '0 9 */3 * *',   label: { es: 'Cada 3 días (9 AM)', en: 'Every 3 days (9 AM)' }, group: 'days' },
  { value: '5d',  cron: '0 9 * * 1-5',   label: { es: 'Lun-Vie (9 AM)', en: 'Mon-Fri (9 AM)' }, group: 'days' },
  // Weeks
  { value: '1w',  cron: '0 9 * * 1',     label: { es: 'Semanal (Lun 9 AM)', en: 'Weekly (Mon 9 AM)' }, group: 'weeks' },
  { value: '2w',  cron: '0 9 1,15 * *',  label: { es: 'Quincenal (1 y 15, 9 AM)', en: 'Bi-weekly (1st & 15th, 9 AM)' }, group: 'weeks' },
  // Month
  { value: '1m',  cron: '0 9 1 * *',     label: { es: 'Mensual (día 1, 9 AM)', en: 'Monthly (1st, 9 AM)' }, group: 'months' },
]

/** Resolve a cron preset value to its expression. Returns null if not found. */
export function cronPresetToCron(presetValue: string): string | null {
  return CRON_PRESETS.find(p => p.value === presetValue)?.cron ?? null
}

/** Find the preset value for a given cron expression. Returns null if custom. */
export function cronToPresetValue(cron: string): string | null {
  return CRON_PRESETS.find(p => p.cron === cron)?.value ?? null
}
```

---

### Cambio A2: API routes valida presets

#### Archivo: `src/modules/scheduled-tasks/api-routes.ts`

**En el handler de CREATE (líneas 120-148):**

Reemplazar la lógica de validación de cron:

```typescript
// ANTES:
if ((!input.trigger_type || input.trigger_type === 'cron') && !input.cron) {
  jsonResponse(res, 400, { error: 'cron is required for cron-triggered tasks' })
  return
}
if (!input.cron) input.cron = '0 0 31 2 *'
if (input.cron && !isValidCron(input.cron)) {
  jsonResponse(res, 400, { error: 'Invalid cron expression' })
  return
}

// DESPUÉS:
if ((!input.trigger_type || input.trigger_type === 'cron') && !input.cron_preset) {
  jsonResponse(res, 400, { error: 'cron_preset is required for cron-triggered tasks' })
  return
}
// Resolve preset to cron expression
if (input.cron_preset) {
  const resolved = cronPresetToCron(input.cron_preset)
  if (!resolved) {
    jsonResponse(res, 400, { error: `Invalid cron preset: ${input.cron_preset}. Valid: ${CRON_PRESETS.map(p => p.value).join(', ')}` })
    return
  }
  input.cron = resolved
} else if (!input.cron) {
  // Non-cron tasks get a placeholder (never scheduled as repeatable per Plan 1)
  input.cron = '0 0 1 1 *'
}
```

Agregar import:
```typescript
import { CRON_PRESETS, cronPresetToCron } from './types.js'
```

**En el handler de UPDATE (líneas 151-183):**

Aplicar la misma lógica de resolución de preset:

```typescript
// If cron_preset provided, resolve to cron
if (input.cron_preset) {
  const resolved = cronPresetToCron(input.cron_preset)
  if (!resolved) {
    jsonResponse(res, 400, { error: `Invalid cron preset: ${input.cron_preset}` })
    return
  }
  input.cron = resolved
}
```

**En la respuesta de CREATE y UPDATE**, agregar `cron_preset` al task object para que la UI sepa qué preset está seleccionado. Esto se puede hacer añadiendo un campo virtual o almacenándolo.

**NOTA**: Para simplicidad, NO almacenar `cron_preset` en la DB. La UI recibe el `cron` string y busca el preset correspondiente via `cronToPresetValue()`. Si no coincide con ningún preset (legacy tasks), la UI muestra "Custom".

**Eliminar o simplificar `isValidCron()`**: Ya no necesitamos validación de cron libre. Podemos mantenerla como fallback para tareas legacy pero no se usa para nuevas tareas.

---

### Cambio A3: Template UI con dropdown de presets

#### Archivo: `src/modules/scheduled-tasks/templates.ts`

**Reemplazar el input de cron** (líneas 346-349) con un dropdown:

```html
<!-- ANTES -->
<div id="st-cron-row">
  <input type="text" id="st-cron" placeholder="*/30 * * * *" class="st-input-sm">
  <div class="st-help">${l('cronHelp', lang)}</div>
</div>

<!-- DESPUÉS -->
<div id="st-cron-row">
  <select id="st-cron-preset" class="st-select">
    ${renderCronPresetOptions(lang)}
  </select>
</div>
```

**Agregar función de render para las opciones:**

```typescript
function renderCronPresetOptions(lang: 'es' | 'en'): string {
  const groups: Record<string, typeof CRON_PRESETS> = {}
  for (const preset of CRON_PRESETS) {
    if (!groups[preset.group]) groups[preset.group] = []
    groups[preset.group]!.push(preset)
  }

  const groupLabels: Record<string, { es: string; en: string }> = {
    minutes: { es: 'Minutos', en: 'Minutes' },
    hours: { es: 'Horas', en: 'Hours' },
    days: { es: 'Días', en: 'Days' },
    weeks: { es: 'Semanas', en: 'Weeks' },
    months: { es: 'Mes', en: 'Month' },
  }

  let html = ''
  for (const [group, presets] of Object.entries(groups)) {
    const label = groupLabels[group]?.[lang] ?? group
    html += `<optgroup label="${label}">`
    for (const p of presets) {
      html += `<option value="${p.value}">${p.label[lang]}</option>`
    }
    html += `</optgroup>`
  }
  return html
}
```

Agregar import de `CRON_PRESETS` al inicio de templates.ts:
```typescript
import { CRON_PRESETS, cronToPresetValue } from './types.js'
```

**Actualizar JavaScript en el template:**

En `stSaveTask()` (función de guardar), cambiar la recolección del cron:

```javascript
// ANTES:
cron: document.getElementById('st-cron').value,

// DESPUÉS:
cron_preset: document.getElementById('st-cron-preset')?.value || null,
```

En `stEditTask()` (función de editar), cargar el preset correspondiente:

```javascript
// ANTES:
document.getElementById('st-cron').value = task.cron

// DESPUÉS:
// Find matching preset for this cron expression
var cronPreset = document.getElementById('st-cron-preset')
if (cronPreset) {
  var presetFound = false
  for (var i = 0; i < cronPreset.options.length; i++) {
    // Compare cron expressions by looking up the data attribute or value
    if (cronPreset.options[i].value === findPresetForCron(task.cron)) {
      cronPreset.selectedIndex = i
      presetFound = true
      break
    }
  }
  if (!presetFound && cronPreset.options.length > 0) {
    cronPreset.selectedIndex = 0 // default to first option
  }
}
```

**Agregar mapa JS de cron→preset:**

En la sección de JavaScript del template, agregar un mapa inline generado desde los presets:

```javascript
var CRON_TO_PRESET = ${JSON.stringify(Object.fromEntries(CRON_PRESETS.map(p => [p.cron, p.value])))}

function findPresetForCron(cron) {
  return CRON_TO_PRESET[cron] || null
}
```

**Actualizar los task cards** para mostrar el label del preset en vez del cron raw:

En la parte donde se muestra el badge del cron (buscar donde renderiza `task.cron`):

```typescript
// Buscar preset label para mostrar en la card
const presetValue = cronToPresetValue(task.cron)
const presetObj = CRON_PRESETS.find(p => p.value === presetValue)
const cronDisplay = presetObj ? presetObj.label[lang] : task.cron
```

Y usar `cronDisplay` en el HTML del badge.

---

### Cambio A4: Actualizar CreateTaskInput

#### Archivo: `src/modules/scheduled-tasks/types.ts`

Agregar `cron_preset` a la interfaz:

```typescript
export interface CreateTaskInput {
  name: string
  prompt: string
  cron?: string         // resolved from preset (or placeholder for non-cron)
  cron_preset?: string  // preset value (e.g. '5min', '1h', '1d')
  enabled?: boolean
  trigger_type?: TriggerType
  trigger_event?: string
  recipient?: TaskRecipient
  actions?: TaskAction[]
}
```

Y lo mismo para `UpdateTaskInput`.

---

## PARTE B: Knowledge contact category map en consola

### Contexto
`KNOWLEDGE_CONTACT_CATEGORY_MAP` es un JSON string que mapea `contact_type` → `categoryId[]`. Hoy solo se puede configurar via env var o insertando directamente en `config_store`. Necesita un campo en la consola.

### Cambio B1: Agregar campo a console.fields

#### Archivo: `src/modules/knowledge/manifest.ts`

En la sección `console.fields` (después de la línea 1236, antes de `apiRoutes`), agregar un nuevo campo en la sección "Avanzado":

```typescript
{
  key: 'KNOWLEDGE_CONTACT_CATEGORY_MAP',
  type: 'textarea',
  label: { es: 'Filtro de categorías por tipo de contacto', en: 'Category filter by contact type' },
  info: {
    es: 'JSON que mapea tipos de contacto a categorías de conocimiento permitidas. Ejemplo: {"lead":["cat-id-1","cat-id-2"],"coworker":["cat-id-3"]}. Vacío = sin filtro.',
    en: 'JSON mapping contact types to allowed knowledge categories. Example: {"lead":["cat-id-1","cat-id-2"],"coworker":["cat-id-3"]}. Empty = no filter.',
  },
  placeholder: '{"lead":["category-uuid-1"],"coworker":["category-uuid-2"]}',
},
```

Insertar este campo **antes** del campo `KNOWLEDGE_SYNC_ENABLED` (línea 1219) o al final de la sección "Avanzado".

**NOTA**: El sistema de config del console ya persiste campos `textarea` en `config_store` automáticamente vía el kernel. No se necesita lógica adicional — el campo se guarda y se lee como cualquier otro campo de consola.

### Cambio B2: Validación del JSON al guardar

No se necesita código nuevo. El `configSchema` ya tiene `KNOWLEDGE_CONTACT_CATEGORY_MAP: z.string().default('')` que acepta cualquier string. La validación del JSON sucede en el handler de `search_knowledge` (líneas 1392-1399) con try/catch. Si el JSON es inválido, fail-open (sin filtro). Esto es suficiente.

**Opcional**: Si queremos validación al guardar en la consola, podemos agregar una API route que valide el JSON. Pero el fail-open actual es seguro y suficiente.

---

## Documentación a actualizar

### `src/modules/scheduled-tasks/CLAUDE.md`
Reescribir sección de triggers para reflejar presets:
```markdown
## Triggers soportados
- **cron** — BullMQ repeatable job. Solo presets fijos (no cron libre):
  - Minutos: 5, 15, 30
  - Horas: 1, 2, 6, 12
  - Días: diario, cada 3 días, lun-vie
  - Semanas: semanal, quincenal
  - Mes: mensual
- **event** — hooks del kernel: contact:new, contact:status_changed, message:incoming, module:activated/deactivated
- **manual** — solo ejecutable via botón o API /trigger
```

Actualizar trampas:
```markdown
- Cron presets definidos en types.ts (CRON_PRESETS). Para agregar nuevos, modificar el array.
- Tareas legacy con cron libre se muestran como "Custom" en la UI pero siguen funcionando.
```

### `src/modules/knowledge/CLAUDE.md`
Agregar en la sección de config:
```markdown
- `KNOWLEDGE_CONTACT_CATEGORY_MAP`: configurable desde consola (campo textarea en sección Avanzado). JSON que mapea contact_type → categoryId[]. Fail-open: si vacío o JSON inválido, no filtra.
```

---

## Verificación

1. **Compilar**: `npx tsc --noEmit` — sin errores
2. **Verificar presets**: Crear una tarea cron en la UI → la API recibe `cron_preset`, lo resuelve a cron expression, y lo guarda
3. **Verificar legacy**: Tareas existentes con cron libre siguen funcionando (el scheduler lee `task.cron`, no el preset)
4. **Verificar knowledge field**: El campo `KNOWLEDGE_CONTACT_CATEGORY_MAP` aparece en la consola bajo Knowledge > Avanzado
5. **Verificar backward compat**: Si `KNOWLEDGE_CONTACT_CATEGORY_MAP` está vacío, search_knowledge no filtra (fail-open)

---

## Resumen de archivos modificados/creados

| Archivo | Cambio |
|---------|--------|
| `src/modules/scheduled-tasks/types.ts` | CRON_PRESETS, cronPresetToCron(), cronToPresetValue(), cron_preset en inputs |
| `src/modules/scheduled-tasks/api-routes.ts` | Validar y resolver cron_preset en CREATE y UPDATE |
| `src/modules/scheduled-tasks/templates.ts` | Dropdown de presets, render optgroups, JS actualizado |
| `src/modules/knowledge/manifest.ts` | Campo `KNOWLEDGE_CONTACT_CATEGORY_MAP` en console.fields |
| `src/modules/scheduled-tasks/CLAUDE.md` | Documentar presets |
| `src/modules/knowledge/CLAUDE.md` | Documentar campo en consola |
