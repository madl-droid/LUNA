# Plan 04 — Audit Fixes + Eliminación S2 + googleApiCall en Docs/Slides

**Módulo:** `src/modules/google-apps/`
**Scope:** Eliminación S2, BUG-1, BUG-2, BUG-3, HUECO-3, HUECO-4, PERF-1, DUP-1, DUP-2, DUP-3, COMPLEX-1, POL-1
**Ejecución:** Un solo ejecutor (todos los cambios son en el mismo módulo y se solapan en archivos)

---

## Archivos a modificar

| Archivo | Cambios |
|---------|---------|
| `tools.ts` | Eliminar isProtectedSheet + guards (S2); fix slides-read doble fetch (BUG-3); try/catch en slides-update-notes (HUECO-3); paginación server-side en sheets-read (PERF-1) |
| `sheets-service.ts` | Extraer lógica de validación a método reutilizable (BUG-2) |
| `docs-service.ts` | Validar searchText en replace (BUG-1); simplificar batchEdit (COMPLEX-1); agregar googleApiCall wrapper (DUP-3) |
| `slides-service.ts` | Extraer helper de text extraction (DUP-1); updateSpeakerNotes usa batchEdit (DUP-2); applied count real (HUECO-4); agregar googleApiCall wrapper (DUP-3) |
| `manifest.ts` | Eliminar GOOGLE_SHEETS_PROTECTED_IDS (S2); pasar config a DocsService y SlidesService (DUP-3) |
| `types.ts` | Eliminar GOOGLE_SHEETS_PROTECTED_IDS de GoogleApiConfig (S2) |
| `CLAUDE.md` | Eliminar sección "Protección de Sheets"; fix activateByDefault (POL-1); documentar googleApiCall en Docs/Slides |

---

## Tarea 1: Eliminar S2 (Protected Sheets) — completo

El mecanismo de protección se elimina porque los permisos de Google Drive ya cubren este caso.

### Paso 1a: types.ts

**Eliminar** la línea `GOOGLE_SHEETS_PROTECTED_IDS: string` de la interfaz `GoogleApiConfig` (línea 16).

### Paso 1b: manifest.ts — configSchema

**Eliminar** la línea de configSchema (línea 394):
```typescript
GOOGLE_SHEETS_PROTECTED_IDS: z.string().default('').describe('Comma-separated spreadsheet IDs protegidos contra escritura'),
```

### Paso 1c: tools.ts — eliminar helper y guards

1. **Eliminar** la función `isProtectedSheet()` completa (líneas 386-391)
2. **Eliminar** el guard de `sheets-write` handler (línea ~521-523):
   ```typescript
   if (isProtectedSheet(spreadsheetId)) {
     return { success: false, error: 'Este spreadsheet está protegido contra escritura por el administrador.' }
   }
   ```
3. **Eliminar** el guard de `sheets-append` handler (misma estructura, buscar `isProtectedSheet`)
4. **Eliminar** el guard de `sheets-find-replace` handler (línea ~629-631)
5. **Eliminar** el guard de `sheets-batch-edit` handler (línea ~666-668)
6. **Eliminar** `GoogleApiConfig` del import de types en línea 11 (ya no se necesita en tools.ts — verificar que ningún otro uso quede)

### Paso 1d: CLAUDE.md

**Eliminar** la sección "Protección de Sheets" completa (que se agregó en Plan 01).
**Actualizar** la línea de Sheets tools removiendo las notas "(protección)":
```
- Sheets: sheets-read (paginación, auto-detect tab), sheets-write, sheets-append (restaura validaciones), sheets-create, sheets-info, sheets-find-replace, sheets-batch-edit
```

---

## Tarea 2: BUG-1 — Validar searchText en docs-batch-edit replace

**Archivo:** `docs-service.ts` — método `batchEdit()` (línea ~170)

Agregar validación **al inicio** del método, ANTES de separar operaciones:

```typescript
async batchEdit(documentId: string, operations: DocEditOperation[]): Promise<{ applied: number }> {
  // Validate: replace ops must have searchText
  const badReplace = operations.find(op => op.type === 'replace' && !op.searchText)
  if (badReplace) {
    throw new Error('Las operaciones replace requieren searchText')
  }
  // ... resto del método
```

Además, corregir la línea 180 que hace fallback inseguro:
```typescript
// ANTES (BUG):
containsText: { text: op.searchText ?? op.text, matchCase: true },
// DESPUÉS (con guard arriba, searchText siempre existe para replace):
containsText: { text: op.searchText!, matchCase: true },
```

---

## Tarea 3: BUG-2 — sheets-batch-edit append restaura validaciones

**Archivo:** `sheets-service.ts` — método `batchEdit()`, sección APPENDS (líneas 268-273)

El problema: `batchEdit` llama `this.appendRows()` directamente, sin restaurar validaciones como hace el tool `sheets-append`.

### Fix

Reemplazar la sección APPENDS en `batchEdit()`:

```typescript
// APPENDS — ejecutar secuencialmente, restaurar validaciones
const appendOps = operations.filter((op) => op.type === 'append' && op.range && op.values)
for (const op of appendOps) {
  const range = op.range!
  const values = op.values!

  // Parsear sheetTitle del range
  const sheetTitleMatch = /^'?([^'!]+)'?!/.exec(range)
  const sheetTitle = sheetTitleMatch?.[1] ?? range.split('!')[0] ?? 'Sheet1'

  // Best-effort: capturar validaciones antes del append
  let sheetId: number | undefined
  let lastDataRow = 0
  let validations: Array<Record<string, unknown> | null> = []
  try {
    const info = await this.getSpreadsheet(spreadsheetId)
    const sheetMeta = info.sheets.find((s) => s.title === sheetTitle)
    sheetId = sheetMeta?.sheetId
    const existing = await this.readRange(spreadsheetId, range)
    lastDataRow = existing.values.length > 0 ? existing.values.length - 1 : 0
    if (sheetId !== undefined && lastDataRow > 0) {
      validations = await this.getRowValidations(spreadsheetId, sheetTitle, lastDataRow)
    }
  } catch {
    // best-effort — continuar sin validaciones
  }

  // Ejecutar append
  const r = await this.appendRows(spreadsheetId, range, values)

  // Restaurar validaciones (fire-and-forget)
  if (sheetId !== undefined && validations.some((v) => v !== null)) {
    this.applyValidations(spreadsheetId, sheetId, validations, lastDataRow + 1, values.length).catch(() => {})
  }

  results.push({ type: 'append', detail: r })
}
```

**Alternativa más limpia:** Extraer la lógica de "append con validaciones" a un método privado `appendWithValidations(spreadsheetId, range, values)` y usarlo tanto en el tool `sheets-append` handler (tools.ts) como en `batchEdit`. Esto evita duplicar la lógica. Si se elige esta ruta:

1. En `sheets-service.ts`: crear método `async appendWithValidations(spreadsheetId, range, values)` que hace todo (parsear title, get validations, append, restore)
2. En `sheets-service.ts batchEdit()`: llamar `this.appendWithValidations()` en vez de `this.appendRows()`
3. En `tools.ts sheets-append handler`: simplificar a solo llamar `sheets.appendWithValidations()` en vez de toda la lógica inline

**Se recomienda la alternativa limpia.** Centraliza la lógica en un solo lugar.

---

## Tarea 4: BUG-3 — slides-read doble fetch

**Archivo:** `tools.ts` — handler de `slides-read` (líneas 859-873)

Estado actual — 2 llamadas API al mismo presentation:
```typescript
const text = await slides.getSlideText(presentationId, slideIndex)    // GET presentations
const info = await slides.getPresentation(presentationId)              // GET presentations OTRA VEZ
```

### Fix

Refactorizar `SlidesService.getSlideText()` para aceptar data pre-fetched, O crear un nuevo método que retorne ambos.

**Opción recomendada:** Modificar `getSlideText()` en `slides-service.ts` para aceptar un parámetro opcional `preloadedSlides`:

```typescript
async getSlideText(
  presentationId: string,
  slideIndex?: number,
  preloadedData?: { slides: unknown[]; title: string },
): Promise<{ text: string; title: string; totalSlides: number }>
```

Lógica:
```
1. Si preloadedData existe, usar directamente
2. Si no, hacer GET como antes
3. Retornar { text, title, totalSlides } en vez de solo string
```

Luego en `tools.ts` handler de `slides-read`:
```typescript
handler: async (input) => {
  const presentationId = input.presentationId as string
  const slideIndex = input.slideIndex as number | undefined
  const result = await slides.getSlideText(presentationId, slideIndex)
  return {
    success: true,
    data: {
      title: result.title,
      totalSlides: result.totalSlides,
      readingSlide: slideIndex !== undefined ? slideIndex + 1 : 'all',
      text: result.text,
    },
  }
},
```

**IMPORTANTE:** Este cambio modifica el return type de `getSlideText()` de `string` a un objeto. Verificar que no haya otros consumidores de este método. Buscar `getSlideText` en todo el codebase. Si hay otros usos que esperan string, agregar un overload o mantener compatibilidad.

---

## Tarea 5: HUECO-3 — try/catch en slides-update-notes handler

**Archivo:** `tools.ts` — handler de `slides-update-notes` (líneas 992-598)

Estado actual — sin catch, excepciones llegan como crashes:
```typescript
handler: async (input) => {
  await slides.updateSpeakerNotes(...)
  return { success: true, data: { updated: true, slideIndex: input.slideIndex } }
},
```

### Fix

```typescript
handler: async (input) => {
  try {
    await slides.updateSpeakerNotes(
      input.presentationId as string,
      input.slideIndex as number,
      input.text as string,
    )
    return { success: true, data: { updated: true, slideIndex: input.slideIndex } }
  } catch (err) {
    return { success: false, error: err instanceof Error ? err.message : String(err) }
  }
},
```

---

## Tarea 6: HUECO-4 — applied count real en batch-edits

### Paso 6a: slides-service.ts — batchEdit (línea 207)

Cambiar:
```typescript
return { applied: operations.length, results }
```
Por:
```typescript
const successCount = results.filter(r => !(r.detail as Record<string, unknown>)?.error).length
return { applied: successCount, results }
```

### Paso 6b: docs-service.ts — batchEdit (línea 259)

Cambiar:
```typescript
return { applied: operations.length }
```
Por:
```typescript
return { applied: operations.length }  // Docs batchEdit no tiene operaciones parcialmente fallidas
                                        // (si una falla, todo el batchUpdate falla)
```

**Nota:** En Docs, el `batchUpdate` de Google es atómico — si una operación falla, ninguna se aplica. Así que `operations.length` es correcto aquí. Solo Slides tiene el caso de operaciones que "skippean" (update_notes en slide inexistente). No cambiar Docs.

---

## Tarea 7: PERF-1 — Paginación server-side en sheets-read

**Archivo:** `tools.ts` — handler de `sheets-read`

El problema: `readRange(spreadsheetId, range)` lee TODO el rango. Para hojas de 100K+ filas, esto es OOM.

### Fix

La Google Sheets API no tiene paginación nativa en `values.get`, PERO podemos calcular un rango acotado en A1 notation.

**Estrategia:**
1. Si el range es algo como `'Sheet1'` (hoja completa sin rango), primero hacer `getSpreadsheet()` para saber cuántas filas tiene (ya lo hacemos para auto-detect)
2. Calcular el rango server-side: `Sheet1!A1:ZZ{offset+limit+1}` (offset + limit + 1 para incluir header)
3. Así solo descargamos las filas necesarias

**PERO** hay un problema: no sabemos cuántas filas totales tiene la hoja sin leer todo. `rowCount` de `SheetProperties` es el total del grid (puede ser 1000 por defecto), no las filas con datos.

**Solución pragmática:** Agregar un **hard limit** de descarga. Si el rango es la hoja completa (sin especificar celdas), limitar a `offset + limit + 1` filas. Si el usuario especifica un rango explícito (ej: `Sheet1!A1:D50000`), respetar lo que pidió.

En el handler, DESPUÉS de resolver auto-detect y ANTES de llamar `readRange`:

```typescript
// Si el range no tiene rango de filas (ej: "'Sheet1'" o "Sheet1"), limitar server-side
// para evitar cargar toda la hoja en memoria
const hasExplicitRows = /\d/.test(range)  // tiene números = tiene rango explícito
if (!hasExplicitRows) {
  // Calcular rango acotado: traer solo offset + limit + 1 filas (header + datos necesarios)
  const maxRow = offset + limit + 2  // +1 para header, +1 para 1-based
  range = `${range}!A1:ZZZ${maxRow}`
}
```

Y en el metadata de respuesta, agregar un campo que indique si hay más datos:
```typescript
// Si leímos exactamente maxRow filas, probablemente hay más
const mayHaveMore = !hasExplicitRows && allValues.length >= offset + limit + 2
```

**Nota:** `ZZZ` como columna final cubre hasta ~18K columnas — más que suficiente. Google Sheets tiene un límite de 18,278 columnas.

---

## Tarea 8: DUP-1 — Extraer helper compartido de text extraction en Slides

**Archivo:** `slides-service.ts`

### Fix

Crear un método privado `extractTextFromElements()` que reemplaza la lógica duplicada entre `extractSlideText()` y `extractSpeakerNotes()`:

```typescript
/**
 * Extrae texto de un array de pageElements (shapes con texto).
 * Usado por extractSlideText (slide content) y extractSpeakerNotes (notes page).
 */
private extractTextFromElements(
  pageElements: Array<Record<string, unknown>>,
  options?: { trimEmpty?: boolean },
): string {
  const parts: string[] = []
  for (const element of pageElements) {
    const shape = element.shape as Record<string, unknown> | undefined
    if (!shape) continue
    const textContent = shape.text as Record<string, unknown> | undefined
    if (!textContent) continue
    const textElements = (textContent.textElements ?? []) as Array<Record<string, unknown>>
    for (const te of textElements) {
      const textRun = te.textRun as Record<string, unknown> | undefined
      if (textRun?.content) {
        const content = String(textRun.content)
        if (!options?.trimEmpty || content.trim()) {
          parts.push(content)
        }
      }
    }
  }
  return options?.trimEmpty ? parts.join('').trim() : parts.join('')
}
```

Luego refactorizar:

```typescript
private extractSlideText(slide: Record<string, unknown>): string {
  const elements = (slide.pageElements ?? []) as Array<Record<string, unknown>>
  return this.extractTextFromElements(elements)
}

private extractSpeakerNotes(slide: Record<string, unknown>): string {
  const slideProps = slide.slideProperties as Record<string, unknown> | undefined
  const notesPage = slideProps?.notesPage as Record<string, unknown> | undefined
  if (!notesPage) return ''
  const elements = (notesPage.pageElements ?? []) as Array<Record<string, unknown>>
  return this.extractTextFromElements(elements, { trimEmpty: true })
}
```

---

## Tarea 9: DUP-2 — updateSpeakerNotes usa batchEdit internamente

**Archivo:** `slides-service.ts` — método `updateSpeakerNotes()` (líneas 102-137)

### Fix

Reemplazar todo el cuerpo por una delegación a `batchEdit`:

```typescript
async updateSpeakerNotes(presentationId: string, slideIndex: number, text: string): Promise<void> {
  const result = await this.batchEdit(presentationId, [
    { type: 'update_notes', slideIndex, text },
  ])
  // Si batchEdit reportó un error en la operación, re-lanzar para que el caller lo vea
  const detail = result.results[0]?.detail as Record<string, unknown> | undefined
  if (detail?.error) {
    throw new Error(String(detail.error))
  }
}
```

Esto elimina ~35 líneas de código duplicado y garantiza que los fixes a batchEdit se apliquen también a updateSpeakerNotes.

---

## Tarea 10: DUP-3 — googleApiCall wrapper en DocsService y SlidesService

### Paso 10a: DocsService

**Archivo:** `docs-service.ts`

1. **Agregar imports:**
   ```typescript
   import type { GoogleApiConfig } from './types.js'
   import { googleApiCall } from './api-wrapper.js'
   ```

2. **Agregar apiConfig al constructor:**
   ```typescript
   export class DocsService {
     private docs
     private apiConfig: { timeoutMs: number; maxRetries: number }

     constructor(auth: OAuth2Client, config?: GoogleApiConfig) {
       this.docs = google.docs({ version: 'v1', auth })
       this.apiConfig = {
         timeoutMs: config?.GOOGLE_API_TIMEOUT_MS ?? 30000,
         maxRetries: config?.GOOGLE_API_RETRY_MAX ?? 2,
       }
     }
   ```

3. **Wrappear las llamadas API existentes.** Para CADA llamada a `this.docs.documents.*()`, cambiar a `googleApiCall(() => this.docs.documents.*(), this.apiConfig, 'docs.*')`. Lista de métodos a actualizar:
   - `getDocument()`: `this.docs.documents.get(...)` → `googleApiCall(() => this.docs.documents.get(...), this.apiConfig, 'docs.documents.get')`
   - `createDocument()`: `this.docs.documents.create(...)` y el `batchUpdate` de inserción inicial
   - `insertText()`: `this.docs.documents.batchUpdate(...)`
   - `replaceText()`: `this.docs.documents.batchUpdate(...)`
   - `appendText()`: `this.docs.documents.get(...)` y `this.docs.documents.batchUpdate(...)`
   - `batchEdit()`: todas las llamadas a `this.docs.documents.batchUpdate(...)` y `this.docs.documents.get(...)`

### Paso 10b: SlidesService

**Archivo:** `slides-service.ts`

1. **Agregar imports:**
   ```typescript
   import type { GoogleApiConfig } from './types.js'
   import { googleApiCall } from './api-wrapper.js'
   ```

2. **Agregar apiConfig al constructor:**
   ```typescript
   export class SlidesService {
     private slides
     private apiConfig: { timeoutMs: number; maxRetries: number }

     constructor(auth: OAuth2Client, config?: GoogleApiConfig) {
       this.slides = google.slides({ version: 'v1', auth })
       this.apiConfig = {
         timeoutMs: config?.GOOGLE_API_TIMEOUT_MS ?? 30000,
         maxRetries: config?.GOOGLE_API_RETRY_MAX ?? 2,
       }
     }
   ```

3. **Wrappear las llamadas API.** Misma lógica que DocsService. Métodos:
   - `getPresentation()`: `this.slides.presentations.get(...)`
   - `createPresentation()`: `this.slides.presentations.create(...)`
   - `getSlideText()`: `this.slides.presentations.get(...)`
   - `addSlide()`: `this.slides.presentations.batchUpdate(...)`
   - `replaceText()`: `this.slides.presentations.batchUpdate(...)`
   - `insertTextInShape()`: `this.slides.presentations.batchUpdate(...)`
   - `batchEdit()`: `this.slides.presentations.get(...)` y `this.slides.presentations.batchUpdate(...)`

### Paso 10c: manifest.ts — pasar config a constructores

**Archivo:** `manifest.ts` — sección init() (líneas 487-495)

Cambiar:
```typescript
if (_enabledSet.has('docs')) {
  _services.docs = new DocsService(authClient)
```
A:
```typescript
if (_enabledSet.has('docs')) {
  _services.docs = new DocsService(authClient, config)
```

Cambiar:
```typescript
if (_enabledSet.has('slides')) {
  _services.slides = new SlidesService(authClient)
```
A:
```typescript
if (_enabledSet.has('slides')) {
  _services.slides = new SlidesService(authClient, config)
```

---

## Tarea 11: COMPLEX-1 — Simplificar docs batchEdit

**Archivo:** `docs-service.ts` — método `batchEdit()` (líneas 166-259)

### Fix

Eliminar el flag `hasMixed` y simplificar a 2 pasos secuenciales:

```typescript
async batchEdit(documentId: string, operations: DocEditOperation[]): Promise<{ applied: number }> {
  // Validate: replace ops must have searchText
  const badReplace = operations.find(op => op.type === 'replace' && !op.searchText)
  if (badReplace) throw new Error('Las operaciones replace requieren searchText')

  const replaceOps = operations.filter(op => op.type === 'replace')
  const insertOps = operations.filter(op => op.type === 'insert')
  const appendOps = operations.filter(op => op.type === 'append')

  // Step 1: Execute replaces (if any) — no index issues
  if (replaceOps.length > 0) {
    const requests = replaceOps.map(op => ({
      replaceAllText: {
        containsText: { text: op.searchText!, matchCase: true },
        replaceText: op.text,
      },
    }))
    await googleApiCall(
      () => this.docs.documents.batchUpdate({ documentId, requestBody: { requests } }),
      this.apiConfig, 'docs.documents.batchUpdate(replace)',
    )
  }

  // Step 2: Execute inserts + appends (if any) — need fresh endIndex
  if (insertOps.length > 0 || appendOps.length > 0) {
    const doc = await googleApiCall(
      () => this.docs.documents.get({ documentId }),
      this.apiConfig, 'docs.documents.get',
    )
    const content = doc.data.body?.content ?? []
    const lastElement = content[content.length - 1]
    const endIndex = lastElement?.endIndex ?? 1

    const requests: object[] = []

    // Inserts: descending order to avoid displacement
    const sortedInserts = [...insertOps].sort((a, b) => (b.index ?? 1) - (a.index ?? 1))
    for (const op of sortedInserts) {
      requests.push({ insertText: { location: { index: op.index ?? 1 }, text: op.text } })
    }

    // Appends: accumulate offset
    let offset = 0
    const baseIndex = Math.max(endIndex - 1, 1)
    for (const op of appendOps) {
      requests.push({ insertText: { location: { index: baseIndex + offset }, text: op.text } })
      offset += op.text.length
    }

    if (requests.length > 0) {
      await googleApiCall(
        () => this.docs.documents.batchUpdate({ documentId, requestBody: { requests } }),
        this.apiConfig, 'docs.documents.batchUpdate(insert+append)',
      )
    }
  }

  return { applied: operations.length }
}
```

Esto elimina: el flag `hasMixed`, el helper `buildReplaceRequests`, el helper `buildInsertAppendRequests`, y las 3 ramas condicionales. Lo reemplaza con 2 pasos secuenciales siempre ejecutados (si hay ops de ese tipo).

**Nota:** Este refactor se combina con Tarea 2 (BUG-1 validation) y Tarea 10a (googleApiCall wrapper). El ejecutor debe implementarlos juntos.

---

## Tarea 12: POL-1 — Fix CLAUDE.md activateByDefault

**Archivo:** `CLAUDE.md` — línea 21

Cambiar:
```
- type: `provider`, removable: true, activateByDefault: false
```
A:
```
- type: `provider`, removable: true, activateByDefault: true
```

---

## Orden de ejecución recomendado

Las tareas tienen dependencias internas. Orden sugerido:

1. **Tarea 1** (eliminar S2) — limpia código, reduce scope de archivos
2. **Tarea 10** (DUP-3: googleApiCall) — cambio estructural en constructores de DocsService/SlidesService
3. **Tarea 8** (DUP-1: extract helper) — prepara para DUP-2
4. **Tarea 9** (DUP-2: updateSpeakerNotes delega) — requiere DUP-1
5. **Tarea 11** (COMPLEX-1: simplificar docs batchEdit) — requiere DUP-3 (usa googleApiCall)
6. **Tarea 2** (BUG-1: validar searchText) — se integra en Tarea 11
7. **Tarea 6** (HUECO-4: applied count) — standalone
8. **Tarea 3** (BUG-2: append con validaciones) — standalone
9. **Tarea 4** (BUG-3: slides-read single fetch) — standalone
10. **Tarea 5** (HUECO-3: try/catch) — standalone
11. **Tarea 7** (PERF-1: paginación server-side) — standalone
12. **Tarea 12** (POL-1: CLAUDE.md) — al final

---

## Validación

Antes de hacer push, el ejecutor DEBE:

1. **Compilar TypeScript:**
   ```bash
   npx tsc --noEmit
   ```
   Verificar que NO hay errores nuevos (solo los preexistentes de node_modules).

2. **Buscar residuos de S2:**
   ```bash
   grep -rn "isProtectedSheet\|PROTECTED_IDS" src/modules/google-apps/
   ```
   Debe retornar 0 resultados.

3. **Verificar que getSlideText ya no retorna string:**
   ```bash
   grep -rn "getSlideText" src/
   ```
   Todos los consumidores deben manejar el nuevo return type.

4. **Verificar que googleApiCall se usa en Docs y Slides:**
   ```bash
   grep -n "googleApiCall" src/modules/google-apps/docs-service.ts src/modules/google-apps/slides-service.ts
   ```
   Ambos archivos deben tener múltiples hits.

5. **Verificar que updateSpeakerNotes delega a batchEdit:**
   El método debe ser ~5 líneas, no ~35.

6. **Verificar CLAUDE.md:**
   - Sin mención de "protección" o "protected"
   - `activateByDefault: true`
   - Documentar que Docs/Slides ahora usan googleApiCall
