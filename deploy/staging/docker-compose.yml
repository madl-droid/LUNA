services:
  app:
    image: ghcr.io/madl-droid/luna:staging
    container_name: LUNA-S
    restart: unless-stopped
    env_file: .env
    ports:
      - "${APP_PORT:-3001}:${APP_PORT:-3001}"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.luna-staging.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.luna-staging.entrypoints=websecure"
      - "traefik.http.routers.luna-staging.tls.certresolver=letsencrypt"
      - "traefik.http.services.luna-staging.loadbalancer.server.port=${APP_PORT:-3001}"
      - "com.centurylinklabs.watchtower.enable=true"
    volumes:
      - ./instance:/app/instance:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: LUNA-S-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-luna}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-luna_staging}
    volumes:
      - luna-s-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-luna}"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: LUNA-S-redis
    restart: unless-stopped
    volumes:
      - luna-s-redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  luna-s-pgdata:
  luna-s-redis:
